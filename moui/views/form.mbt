///|
/// Aggregate facade for the form/ sub-package (merged from the per-file
/// form_*.mbt / datepicker.mbt facades).
pub using @form {
  datepicker,
  drop_zone,
  file_import_panel,
  form,
  form_actions,
  form_error,
  form_field,
  form_field_state,
  form_helper_text,
  form_section,
  form_validation_summary,
  form_workflow_bar,
  required_field,
  validate_form,
  type BaselineFormRow,
  type FileImportAvailability,
  type FileSelection,
  type FormController,
  type FormFieldError,
  type FormFieldLayout,
  type FormFieldState,
  type FormFieldStatus,
  type FormValidationDisplay,
  type FormValidationResult,
  type FormValidationRule,
  type FormValidationSummary,
}
