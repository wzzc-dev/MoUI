name: Real Skia Acceptance

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - ".github/workflows/real-skia-acceptance.yml"
      - "native/**"
      - "scripts/**"
      - "*.mbt"
      - "moon.mod"
      - "moon.pkg"
      - "skia-provider-lock.json"
      - "skia-platform-status.json"
  schedule:
    - cron: "41 4 * * 2"

permissions:
  contents: read

jobs:
  linux:
    name: Linux JetBrains Skia acceptance
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up MoonBit
        shell: bash
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: Show MoonBit version
        shell: bash
        run: moon version --all

      - name: Update MoonBit registry
        shell: bash
        run: moon update

      - name: Cache JetBrains Skia
        uses: actions/cache@v4
        with:
          path: .skia-cache/jetbrains
          key: jetbrains-skia-linux-${{ runner.arch }}-${{ hashFiles('skia-provider-lock.json') }}
          restore-keys: |
            jetbrains-skia-linux-${{ runner.arch }}-
            jetbrains-skia-linux-

      - name: Run real Skia acceptance
        shell: bash
        run: |
          mkdir -p logs
          set -o pipefail
          bash scripts/linux-real-skia-smoke.sh \
            --skia-provider jetbrains \
            --dry-run-config 2>&1 | tee logs/linux-real-skia-smoke-preflight.log
          bash scripts/linux-accept-real-skia-smoke.sh \
            --log-dir logs \
            --skia-provider jetbrains

      - name: Verify real Skia artifact bundle
        shell: bash
        run: bash scripts/verify-real-skia-artifact.sh --platform linux --log-dir logs

      - name: Verify platform status can be accepted
        shell: bash
        run: |
          before_status="$(mktemp)"
          cp skia-platform-status.json "$before_status"
          bash scripts/accept-platform-status.sh \
            --platform linux \
            --log-dir logs \
            --artifact-label "github-actions:${GITHUB_RUN_ID}:linux-real-skia-acceptance"
          git diff -- skia-platform-status.json > logs/linux-platform-acceptance.patch
          cp "$before_status" skia-platform-status.json
          rm -f "$before_status"
          test -s logs/linux-platform-acceptance.patch

      - name: Verify package files restored
        if: ${{ always() }}
        shell: bash
        run: git diff --exit-code -- native/moon.pkg scripts/native_smoke/moon.pkg

      - name: Upload Linux acceptance logs
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: linux-real-skia-acceptance-log
          path: |
            logs/linux-real-skia-smoke-preflight.log
            logs/linux-skia-build.log
            logs/linux-real-skia-smoke.log
            logs/linux-native-smoke-output.log
            logs/linux-real-skia-acceptance.log
            logs/linux-platform-acceptance.patch
          if-no-files-found: warn

  macos:
    name: macOS JetBrains Skia acceptance
    runs-on: macos-latest
    timeout-minutes: 90

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up MoonBit
        shell: bash
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: Show MoonBit version
        shell: bash
        run: moon version --all

      - name: Update MoonBit registry
        shell: bash
        run: moon update

      - name: Install build dependencies
        shell: bash
        run: brew install ninja

      - name: Cache JetBrains Skia
        uses: actions/cache@v4
        with:
          path: .skia-cache/jetbrains
          key: jetbrains-skia-macos-${{ runner.arch }}-${{ hashFiles('skia-provider-lock.json') }}
          restore-keys: |
            jetbrains-skia-macos-${{ runner.arch }}-
            jetbrains-skia-macos-

      - name: Run real Skia acceptance
        shell: bash
        run: |
          bash scripts/macos-accept-real-skia-smoke.sh \
            --log-dir logs \
            --skia-provider jetbrains

      - name: Verify real Skia artifact bundle
        shell: bash
        run: bash scripts/verify-real-skia-artifact.sh --platform macos --log-dir logs

      - name: Verify platform status can be accepted
        shell: bash
        run: |
          before_status="$(mktemp)"
          cp skia-platform-status.json "$before_status"
          bash scripts/accept-platform-status.sh \
            --platform macos \
            --log-dir logs \
            --artifact-label "github-actions:${GITHUB_RUN_ID}:macos-real-skia-acceptance"
          git diff -- skia-platform-status.json > logs/macos-platform-acceptance.patch
          cp "$before_status" skia-platform-status.json
          rm -f "$before_status"
          test -s logs/macos-platform-acceptance.patch

      - name: Verify package files restored
        if: ${{ always() }}
        shell: bash
        run: git diff --exit-code -- native/moon.pkg scripts/native_smoke/moon.pkg

      - name: Upload macOS acceptance logs
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: macos-real-skia-acceptance-log
          path: |
            logs/macos-real-skia-smoke-preflight.log
            logs/macos-skia-build.log
            logs/macos-real-skia-smoke.log
            logs/macos-native-smoke-output.log
            logs/macos-real-skia-acceptance.log
            logs/macos-platform-acceptance.patch
          if-no-files-found: warn

  windows:
    name: Windows MSVC JetBrains Skia acceptance
    runs-on: windows-latest
    timeout-minutes: 90

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up MoonBit
        shell: pwsh
        run: |
          irm https://cli.moonbitlang.com/install/powershell.ps1 | iex
          "$env:USERPROFILE\.moon\bin" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Show MoonBit version
        shell: pwsh
        run: moon version --all

      - name: Update MoonBit registry
        shell: pwsh
        run: moon update

      - name: Resolve Visual Studio environment
        shell: pwsh
        run: |
          $vswhere = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"
          if (!(Test-Path -LiteralPath $vswhere -PathType Leaf)) {
            throw "vswhere.exe was not found: $vswhere"
          }
          $installPath = & $vswhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          if ([string]::IsNullOrWhiteSpace($installPath)) {
            throw "Visual Studio C++ tools were not found"
          }
          $vcvarsall = Join-Path $installPath "VC\Auxiliary\Build\vcvarsall.bat"
          if (!(Test-Path -LiteralPath $vcvarsall -PathType Leaf)) {
            throw "vcvarsall.bat was not found: $vcvarsall"
          }
          "VCVARSALL=$vcvarsall" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Cache JetBrains Skia
        uses: actions/cache@v4
        with:
          path: .skia-cache/jetbrains
          key: jetbrains-skia-windows-${{ runner.arch }}-${{ hashFiles('skia-provider-lock.json') }}
          restore-keys: |
            jetbrains-skia-windows-${{ runner.arch }}-
            jetbrains-skia-windows-

      - name: Run real Skia acceptance
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path logs | Out-Null
          $envLines = .\scripts\fetch-jetbrains-skia.ps1 `
            -Platform windows `
            -Arch auto `
            -Config Release `
            -PrintEnv
          $envMap = @{}
          foreach ($line in $envLines) {
            if ($line -match '^([^=]+)=(.*)$') {
              $envMap[$Matches[1]] = $Matches[2]
            }
          }
          $acceptArgs = @(
            "-LogDir", "logs",
            "-SkiaRoot", $envMap["SKIA_MBT_SKIA_ROOT"],
            "-SkiaInclude", $envMap["SKIA_MBT_SKIA_INCLUDE"],
            "-SkiaLibDir", $envMap["SKIA_MBT_SKIA_LIB_DIR"],
            "-SkiaProvider", "jetbrains",
            "-JetBrainsTag", $envMap["SKIA_MBT_JETBRAINS_TAG"],
            "-SkiaCommit", $envMap["SKIA_MBT_SKIA_COMMIT"],
            "-SkiaPackage", $envMap["SKIA_MBT_SKIA_PACKAGE"],
            "-SkiaPackageSha256", $envMap["SKIA_MBT_SKIA_PACKAGE_SHA256"],
            "-VcVarsAll", $env:VCVARSALL,
            "-VcArch", "x64"
          )
          if (![string]::IsNullOrWhiteSpace($envMap["SKIA_MBT_EXTRA_CC_FLAGS"])) {
            $acceptArgs += @("-ExtraCcFlags", $envMap["SKIA_MBT_EXTRA_CC_FLAGS"])
          }
          if (![string]::IsNullOrWhiteSpace($envMap["SKIA_MBT_EXTRA_LINK_FLAGS"])) {
            $acceptArgs += @("-ExtraLinkFlags", $envMap["SKIA_MBT_EXTRA_LINK_FLAGS"])
          }
          .\scripts\windows-msvc-accept-real-skia-smoke.ps1 @acceptArgs

      - name: Verify real Skia artifact bundle
        shell: pwsh
        run: .\scripts\verify-real-skia-artifact.ps1 -Platform windows -LogDir logs

      - name: Verify platform status can be accepted
        shell: pwsh
        run: |
          $beforeStatus = Get-Content -LiteralPath skia-platform-status.json -Raw
          .\scripts\accept-platform-status.ps1 `
            -Platform windows `
            -LogDir logs `
            -ArtifactLabel "github-actions:${env:GITHUB_RUN_ID}:windows-real-skia-acceptance"
          git diff -- skia-platform-status.json | Out-File -FilePath logs\windows-platform-acceptance.patch -Encoding utf8
          [System.IO.File]::WriteAllText(
            (Resolve-Path -LiteralPath skia-platform-status.json).Path,
            $beforeStatus,
            [System.Text.UTF8Encoding]::new($false)
          )
          if ((Get-Item -LiteralPath logs\windows-platform-acceptance.patch).Length -eq 0) {
            throw "windows-platform-acceptance.patch was empty"
          }

      - name: Verify package files restored
        if: ${{ always() }}
        shell: pwsh
        run: git diff --exit-code -- native/moon.pkg scripts/native_smoke/moon.pkg

      - name: Upload Windows acceptance logs
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: windows-real-skia-acceptance-log
          path: |
            logs/windows-real-skia-smoke-preflight.log
            logs/windows-real-skia-smoke.log
            logs/windows-native-smoke-output.log
            logs/windows-real-skia-acceptance.log
            logs/windows-platform-acceptance.patch
          if-no-files-found: warn
