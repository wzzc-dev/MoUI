# 2026-07-14: MoonBit 0.10.4 toolchain upgrade

- **Agent**: ZCode / Grok
- **Goal**: Upgrade the repository from MoonBit 0.10.3 to 0.10.4 and update related code and docs.
- **Outcome**: Success for `moon check --target native` (0 errors; residual unused-package / Show-deprecation warnings remain).

## Summary

Pinned CI/local toolchain to `moonc v0.10.4+ade96c819`, fixed first-party empty-brace ambiguity for maps/JSON/blocks, corrected multi-window size typing, and vendored a temporary `mizchi/image` Debug patch so dependency checks work under the new `assert_eq : Eq + Debug` requirement.

## Changes Made

| Package/File | What Changed | Why |
|---|---|---|
| `.moonbit-toolchain` | `0.10.3+16975d007` → `0.10.4+ade96c819` | Pin CI installer to 0.10.4 |
| `.agents/skills/osc2026-guide/SKILL.md` | Minimum toolchain floor 0.10.3 → 0.10.4 | Keep contest env guidance current |
| `moon.work` | Add `./third_party/mizchi_image` | Override registry `mizchi/image` with local patch |
| `third_party/mizchi_image/**` | Vendored `mizchi/image@0.4.2` + `derive(Debug)` | Upstream 0.4.2 fails `assert_eq` without `Debug` |
| `examples/multi_window/{linux,windows}_skia/main.mbt` | `@core.Size` → `@geometry.Size` | Missing import / wrong package alias |
| First-party empty `{}` sites | `Map([])` / `Json::empty_object()` / `{ () }` | Warning 0082 ambiguous braces on 0.10.4 |
| `docs/development.md` | Document pin, empty-brace style, image override | Source-of-truth toolchain notes |

## Key Decisions

- Keep warnings non-fatal for pre-existing unused imports and Show→Debug deprecations outside the upgrade blockers.
- Prefer a workspace-local image override over waiting for an upstream mooncakes release, with an explicit removal note.

## Validation

```sh
moon version --all
# moonc v0.10.4+ade96c819

moon check --target native
# Finished ... (28 warnings, 0 errors)
```

## Follow-Up

- [ ] Drop `./third_party/mizchi_image` when `mizchi/image` publishes a 0.10.4-compatible release.
- [ ] Optionally migrate remaining `inspect` / Show-based tests to `Debug` / `debug_inspect`.
- [ ] Clean unused `moon.pkg` imports flagged by warning 0029 in example entrypoints.
