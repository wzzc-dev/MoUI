// Generated by `wit-bindgen` 0.57.1. DO NOT EDIT!

#doc(hidden)
pub fn wasmExportRender() -> Int {

      let (result) : (@uiTypes.UiTree) = render();
      let return_area = mbt_ffi_malloc(8)

      let address = mbt_ffi_malloc(((result).nodes).length() * 88);
      for index = 0; index < ((result).nodes).length(); index = index + 1 {
            let iter_elem : @uiTypes.FlatNode = ((result).nodes)[(index)]
            let iter_base = address + (index * 88);
            mbt_ffi_store32((iter_base) + 0, ((iter_elem).id).reinterpret_as_int())

            match ((iter_elem).parent_id) {
                  None => {
                        mbt_ffi_store8((iter_base) + 4, (0))

                        ()
                  }
                  Some(payload0) => {
                        mbt_ffi_store8((iter_base) + 4, (1))
                        mbt_ffi_store32((iter_base) + 8, (payload0).reinterpret_as_int())

                        ()
                  }
            }
            mbt_ffi_store8((iter_base) + 12, (iter_elem).kind.ordinal())

            match (((iter_elem).props).width) {
                  None => {
                        mbt_ffi_store8((iter_base) + 16, (0))

                        ()
                  }
                  Some(payload2) => {
                        mbt_ffi_store8((iter_base) + 16, (1))
                        mbt_ffi_storef32((iter_base) + 20, payload2)

                        ()
                  }
            }

            match (((iter_elem).props).height) {
                  None => {
                        mbt_ffi_store8((iter_base) + 24, (0))

                        ()
                  }
                  Some(payload4) => {
                        mbt_ffi_store8((iter_base) + 24, (1))
                        mbt_ffi_storef32((iter_base) + 28, payload4)

                        ()
                  }
            }

            match (((iter_elem).props).padding) {
                  None => {
                        mbt_ffi_store8((iter_base) + 32, (0))

                        ()
                  }
                  Some(payload6) => {
                        mbt_ffi_store8((iter_base) + 32, (1))
                        mbt_ffi_storef32((iter_base) + 36, payload6)

                        ()
                  }
            }

            match (((iter_elem).props).background_color) {
                  None => {
                        mbt_ffi_store8((iter_base) + 40, (0))

                        ()
                  }
                  Some(payload8) => {
                        mbt_ffi_store8((iter_base) + 40, (1))
                        mbt_ffi_store8((iter_base) + 41, ((payload8).r).to_int())
                        mbt_ffi_store8((iter_base) + 42, ((payload8).g).to_int())
                        mbt_ffi_store8((iter_base) + 43, ((payload8).b).to_int())
                        mbt_ffi_store8((iter_base) + 44, ((payload8).a).to_int())

                        ()
                  }
            }

            match (((iter_elem).props).text) {
                  None => {
                        mbt_ffi_store8((iter_base) + 48, (0))

                        ()
                  }
                  Some(payload10) => {
                        mbt_ffi_store8((iter_base) + 48, (1))

                        let ptr = mbt_ffi_str2ptr(payload10)
                        mbt_ffi_store32((iter_base) + 56, payload10.length())
                        mbt_ffi_store32((iter_base) + 52, ptr)

                        ()
                  }
            }

            match (((iter_elem).props).value) {
                  None => {
                        mbt_ffi_store8((iter_base) + 60, (0))

                        ()
                  }
                  Some(payload12) => {
                        mbt_ffi_store8((iter_base) + 60, (1))

                        let ptr13 = mbt_ffi_str2ptr(payload12)
                        mbt_ffi_store32((iter_base) + 68, payload12.length())
                        mbt_ffi_store32((iter_base) + 64, ptr13)

                        ()
                  }
            }

            match (((iter_elem).props).on_click) {
                  None => {
                        mbt_ffi_store8((iter_base) + 72, (0))

                        ()
                  }
                  Some(payload15) => {
                        mbt_ffi_store8((iter_base) + 72, (1))
                        mbt_ffi_store32((iter_base) + 76, (payload15).reinterpret_as_int())

                        ()
                  }
            }

            match (((iter_elem).props).on_input) {
                  None => {
                        mbt_ffi_store8((iter_base) + 80, (0))

                        ()
                  }
                  Some(payload17) => {
                        mbt_ffi_store8((iter_base) + 80, (1))
                        mbt_ffi_store32((iter_base) + 84, (payload17).reinterpret_as_int())

                        ()
                  }
            }

      }
      mbt_ffi_store32((return_area) + 4, ((result).nodes).length())
      mbt_ffi_store32((return_area) + 0, address)
      let ret = return_area
      return ret

}

#doc(hidden)
pub fn wasmExportRenderPostReturn(p0 : Int) -> Unit {

      for index = 0; index < (mbt_ffi_load32((p0) + 4)); index = index + 1 {
            let iter_base = (mbt_ffi_load32((p0) + 0)) + (index * 88)

            match (mbt_ffi_load8_u((iter_base) + 48)) {
                  0 => ()
                  1 => {
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 52))

                  }
                  _ => panic()
            }

            match (mbt_ffi_load8_u((iter_base) + 60)) {
                  0 => ()
                  1 => {
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 64))

                  }
                  _ => panic()
            }

      }
      mbt_ffi_free(mbt_ffi_load32((p0) + 0))

}

#doc(hidden)
pub fn wasmExportHandleEvent(p0 : Int, p1 : Int, p2 : Int, p3 : Int) -> Int {

      let lifted : String? = match p1 {
            0 => Option::None
            1 => {

                  let result = mbt_ffi_ptr2str(p2, p3)

                  Option::Some(result)
            }
            _ => panic()
      }

      let (result0) : (@uiTypes.UiTree) = handle_event((p0).reinterpret_as_uint(), lifted);
      let return_area = mbt_ffi_malloc(8)

      let address = mbt_ffi_malloc(((result0).nodes).length() * 88);
      for index = 0; index < ((result0).nodes).length(); index = index + 1 {
            let iter_elem : @uiTypes.FlatNode = ((result0).nodes)[(index)]
            let iter_base = address + (index * 88);
            mbt_ffi_store32((iter_base) + 0, ((iter_elem).id).reinterpret_as_int())

            match ((iter_elem).parent_id) {
                  None => {
                        mbt_ffi_store8((iter_base) + 4, (0))

                        ()
                  }
                  Some(payload1) => {
                        mbt_ffi_store8((iter_base) + 4, (1))
                        mbt_ffi_store32((iter_base) + 8, (payload1).reinterpret_as_int())

                        ()
                  }
            }
            mbt_ffi_store8((iter_base) + 12, (iter_elem).kind.ordinal())

            match (((iter_elem).props).width) {
                  None => {
                        mbt_ffi_store8((iter_base) + 16, (0))

                        ()
                  }
                  Some(payload3) => {
                        mbt_ffi_store8((iter_base) + 16, (1))
                        mbt_ffi_storef32((iter_base) + 20, payload3)

                        ()
                  }
            }

            match (((iter_elem).props).height) {
                  None => {
                        mbt_ffi_store8((iter_base) + 24, (0))

                        ()
                  }
                  Some(payload5) => {
                        mbt_ffi_store8((iter_base) + 24, (1))
                        mbt_ffi_storef32((iter_base) + 28, payload5)

                        ()
                  }
            }

            match (((iter_elem).props).padding) {
                  None => {
                        mbt_ffi_store8((iter_base) + 32, (0))

                        ()
                  }
                  Some(payload7) => {
                        mbt_ffi_store8((iter_base) + 32, (1))
                        mbt_ffi_storef32((iter_base) + 36, payload7)

                        ()
                  }
            }

            match (((iter_elem).props).background_color) {
                  None => {
                        mbt_ffi_store8((iter_base) + 40, (0))

                        ()
                  }
                  Some(payload9) => {
                        mbt_ffi_store8((iter_base) + 40, (1))
                        mbt_ffi_store8((iter_base) + 41, ((payload9).r).to_int())
                        mbt_ffi_store8((iter_base) + 42, ((payload9).g).to_int())
                        mbt_ffi_store8((iter_base) + 43, ((payload9).b).to_int())
                        mbt_ffi_store8((iter_base) + 44, ((payload9).a).to_int())

                        ()
                  }
            }

            match (((iter_elem).props).text) {
                  None => {
                        mbt_ffi_store8((iter_base) + 48, (0))

                        ()
                  }
                  Some(payload11) => {
                        mbt_ffi_store8((iter_base) + 48, (1))

                        let ptr = mbt_ffi_str2ptr(payload11)
                        mbt_ffi_store32((iter_base) + 56, payload11.length())
                        mbt_ffi_store32((iter_base) + 52, ptr)

                        ()
                  }
            }

            match (((iter_elem).props).value) {
                  None => {
                        mbt_ffi_store8((iter_base) + 60, (0))

                        ()
                  }
                  Some(payload13) => {
                        mbt_ffi_store8((iter_base) + 60, (1))

                        let ptr14 = mbt_ffi_str2ptr(payload13)
                        mbt_ffi_store32((iter_base) + 68, payload13.length())
                        mbt_ffi_store32((iter_base) + 64, ptr14)

                        ()
                  }
            }

            match (((iter_elem).props).on_click) {
                  None => {
                        mbt_ffi_store8((iter_base) + 72, (0))

                        ()
                  }
                  Some(payload16) => {
                        mbt_ffi_store8((iter_base) + 72, (1))
                        mbt_ffi_store32((iter_base) + 76, (payload16).reinterpret_as_int())

                        ()
                  }
            }

            match (((iter_elem).props).on_input) {
                  None => {
                        mbt_ffi_store8((iter_base) + 80, (0))

                        ()
                  }
                  Some(payload18) => {
                        mbt_ffi_store8((iter_base) + 80, (1))
                        mbt_ffi_store32((iter_base) + 84, (payload18).reinterpret_as_int())

                        ()
                  }
            }

      }
      mbt_ffi_store32((return_area) + 4, ((result0).nodes).length())
      mbt_ffi_store32((return_area) + 0, address)
      let ret = return_area
      return ret

}

#doc(hidden)
pub fn wasmExportHandleEventPostReturn(p0 : Int) -> Unit {

      for index = 0; index < (mbt_ffi_load32((p0) + 4)); index = index + 1 {
            let iter_base = (mbt_ffi_load32((p0) + 0)) + (index * 88)

            match (mbt_ffi_load8_u((iter_base) + 48)) {
                  0 => ()
                  1 => {
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 52))

                  }
                  _ => panic()
            }

            match (mbt_ffi_load8_u((iter_base) + 60)) {
                  0 => ()
                  1 => {
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 64))

                  }
                  _ => panic()
            }

      }
      mbt_ffi_free(mbt_ffi_load32((p0) + 0))

}

///|
#owned(str)
extern "wasm" fn mbt_ffi_str2ptr(str : String) -> Int =
#|(func (param i32) (result i32) local.get 0 i32.const 8 i32.add)

///|
extern "wasm" fn mbt_ffi_store32(offset : Int, value : Int) =
#|(func (param i32) (param i32) local.get 0 local.get 1 i32.store)

///|
extern "wasm" fn mbt_ffi_malloc(size : Int) -> Int =
#|(func (param i32) (result i32) (local i32)
#| local.get 0 i32.const 4 i32.add call $moonbit.gc.malloc
#| local.tee 1 i32.const 0 call $moonbit.init_array8
#| local.get 1 i32.const 8 i32.add)

///|
extern "wasm" fn mbt_ffi_free(position : Int) =
#|(func (param i32) local.get 0 i32.const 8 i32.sub call $moonbit.decref)

///|
extern "wasm" fn mbt_ffi_ptr2str(ptr : Int, len : Int) -> String =
#|(func (param i32) (param i32) (result i32) (local i32)
#| local.get 0 i32.const 8 i32.sub local.tee 2
#| local.get 1 call $moonbit.init_array16
#| local.get 2)

///|
extern "wasm" fn mbt_ffi_store8(offset : Int, value : Int) =
#|(func (param i32) (param i32) local.get 0 local.get 1 i32.store8)

///|
extern "wasm" fn mbt_ffi_load8_u(offset : Int) -> Int =
#|(func (param i32) (result i32) local.get 0 i32.load8_u)

///|
extern "wasm" fn mbt_ffi_load32(offset : Int) -> Int =
#|(func (param i32) (result i32) local.get 0 i32.load)

///|
extern "wasm" fn mbt_ffi_storef32(offset : Int, value : Float) =
#|(func (param i32) (param f32) local.get 0 local.get 1 f32.store)

