///| Generated by scripts/generate-grapheme-property-data.mjs.

///| Sources: Unicode 17.0.0 GraphemeBreakProperty.txt, emoji-data.txt, and

///| DerivedCoreProperties.txt.

///| Unicode data terms: https://www.unicode.org/terms_of_use.html

///|
fn unicode_grapheme_is_cr_17_0(codepoint : Int) -> Bool {
  codepoint == 0x000D
}

///|
fn unicode_grapheme_is_lf_17_0(codepoint : Int) -> Bool {
  codepoint == 0x000A
}

///|
fn unicode_grapheme_is_control_17_0(codepoint : Int) -> Bool {
  (codepoint >= 0x0000 && codepoint <= 0x0009) ||
  (codepoint >= 0x000B && codepoint <= 0x000C) ||
  (codepoint >= 0x000E && codepoint <= 0x001F) ||
  (codepoint >= 0x007F && codepoint <= 0x009F) ||
  codepoint == 0x00AD ||
  codepoint == 0x061C ||
  codepoint == 0x180E ||
  codepoint == 0x200B ||
  (codepoint >= 0x200E && codepoint <= 0x200F) ||
  (codepoint >= 0x2028 && codepoint <= 0x202E) ||
  (codepoint >= 0x2060 && codepoint <= 0x206F) ||
  codepoint == 0xFEFF ||
  (codepoint >= 0xFFF0 && codepoint <= 0xFFFB) ||
  (codepoint >= 0x13430 && codepoint <= 0x1343F) ||
  (codepoint >= 0x1BCA0 && codepoint <= 0x1BCA3) ||
  (codepoint >= 0x1D173 && codepoint <= 0x1D17A) ||
  (codepoint >= 0xE0000 && codepoint <= 0xE001F) ||
  (codepoint >= 0xE0080 && codepoint <= 0xE00FF) ||
  (codepoint >= 0xE01F0 && codepoint <= 0xE0FFF)
}

///|
fn unicode_grapheme_is_prepend_17_0(codepoint : Int) -> Bool {
  (codepoint >= 0x0600 && codepoint <= 0x0605) ||
  codepoint == 0x06DD ||
  codepoint == 0x070F ||
  (codepoint >= 0x0890 && codepoint <= 0x0891) ||
  codepoint == 0x08E2 ||
  codepoint == 0x0D4E ||
  codepoint == 0x110BD ||
  codepoint == 0x110CD ||
  (codepoint >= 0x111C2 && codepoint <= 0x111C3) ||
  codepoint == 0x113D1 ||
  codepoint == 0x1193F ||
  codepoint == 0x11941 ||
  (codepoint >= 0x11A84 && codepoint <= 0x11A89) ||
  codepoint == 0x11D46 ||
  codepoint == 0x11F02
}

///|
fn unicode_grapheme_is_extend_17_0(codepoint : Int) -> Bool {
  unicode_grapheme_is_extend_17_0_chunk_0(codepoint) ||
  unicode_grapheme_is_extend_17_0_chunk_1(codepoint) ||
  unicode_grapheme_is_extend_17_0_chunk_2(codepoint) ||
  unicode_grapheme_is_extend_17_0_chunk_3(codepoint) ||
  unicode_grapheme_is_extend_17_0_chunk_4(codepoint) ||
  unicode_grapheme_is_extend_17_0_chunk_5(codepoint) ||
  unicode_grapheme_is_extend_17_0_chunk_6(codepoint) ||
  unicode_grapheme_is_extend_17_0_chunk_7(codepoint)
}

///|
fn unicode_grapheme_is_extend_17_0_chunk_0(codepoint : Int) -> Bool {
  (codepoint >= 0x0300 && codepoint <= 0x036F) ||
  (codepoint >= 0x0483 && codepoint <= 0x0489) ||
  (codepoint >= 0x0591 && codepoint <= 0x05BD) ||
  codepoint == 0x05BF ||
  (codepoint >= 0x05C1 && codepoint <= 0x05C2) ||
  (codepoint >= 0x05C4 && codepoint <= 0x05C5) ||
  codepoint == 0x05C7 ||
  (codepoint >= 0x0610 && codepoint <= 0x061A) ||
  (codepoint >= 0x064B && codepoint <= 0x065F) ||
  codepoint == 0x0670 ||
  (codepoint >= 0x06D6 && codepoint <= 0x06DC) ||
  (codepoint >= 0x06DF && codepoint <= 0x06E4) ||
  (codepoint >= 0x06E7 && codepoint <= 0x06E8) ||
  (codepoint >= 0x06EA && codepoint <= 0x06ED) ||
  codepoint == 0x0711 ||
  (codepoint >= 0x0730 && codepoint <= 0x074A) ||
  (codepoint >= 0x07A6 && codepoint <= 0x07B0) ||
  (codepoint >= 0x07EB && codepoint <= 0x07F3) ||
  codepoint == 0x07FD ||
  (codepoint >= 0x0816 && codepoint <= 0x0819) ||
  (codepoint >= 0x081B && codepoint <= 0x0823) ||
  (codepoint >= 0x0825 && codepoint <= 0x0827) ||
  (codepoint >= 0x0829 && codepoint <= 0x082D) ||
  (codepoint >= 0x0859 && codepoint <= 0x085B) ||
  (codepoint >= 0x0897 && codepoint <= 0x089F) ||
  (codepoint >= 0x08CA && codepoint <= 0x08E1) ||
  (codepoint >= 0x08E3 && codepoint <= 0x0902) ||
  codepoint == 0x093A ||
  codepoint == 0x093C ||
  (codepoint >= 0x0941 && codepoint <= 0x0948) ||
  codepoint == 0x094D ||
  (codepoint >= 0x0951 && codepoint <= 0x0957) ||
  (codepoint >= 0x0962 && codepoint <= 0x0963) ||
  codepoint == 0x0981 ||
  codepoint == 0x09BC ||
  codepoint == 0x09BE ||
  (codepoint >= 0x09C1 && codepoint <= 0x09C4) ||
  codepoint == 0x09CD ||
  codepoint == 0x09D7 ||
  (codepoint >= 0x09E2 && codepoint <= 0x09E3) ||
  codepoint == 0x09FE ||
  (codepoint >= 0x0A01 && codepoint <= 0x0A02) ||
  codepoint == 0x0A3C ||
  (codepoint >= 0x0A41 && codepoint <= 0x0A42) ||
  (codepoint >= 0x0A47 && codepoint <= 0x0A48) ||
  (codepoint >= 0x0A4B && codepoint <= 0x0A4D) ||
  codepoint == 0x0A51 ||
  (codepoint >= 0x0A70 && codepoint <= 0x0A71)
}

///|
fn unicode_grapheme_is_extend_17_0_chunk_1(codepoint : Int) -> Bool {
  codepoint == 0x0A75 ||
  (codepoint >= 0x0A81 && codepoint <= 0x0A82) ||
  codepoint == 0x0ABC ||
  (codepoint >= 0x0AC1 && codepoint <= 0x0AC5) ||
  (codepoint >= 0x0AC7 && codepoint <= 0x0AC8) ||
  codepoint == 0x0ACD ||
  (codepoint >= 0x0AE2 && codepoint <= 0x0AE3) ||
  (codepoint >= 0x0AFA && codepoint <= 0x0AFF) ||
  codepoint == 0x0B01 ||
  codepoint == 0x0B3C ||
  (codepoint >= 0x0B3E && codepoint <= 0x0B3F) ||
  (codepoint >= 0x0B41 && codepoint <= 0x0B44) ||
  codepoint == 0x0B4D ||
  (codepoint >= 0x0B55 && codepoint <= 0x0B57) ||
  (codepoint >= 0x0B62 && codepoint <= 0x0B63) ||
  codepoint == 0x0B82 ||
  codepoint == 0x0BBE ||
  codepoint == 0x0BC0 ||
  codepoint == 0x0BCD ||
  codepoint == 0x0BD7 ||
  codepoint == 0x0C00 ||
  codepoint == 0x0C04 ||
  codepoint == 0x0C3C ||
  (codepoint >= 0x0C3E && codepoint <= 0x0C40) ||
  (codepoint >= 0x0C46 && codepoint <= 0x0C48) ||
  (codepoint >= 0x0C4A && codepoint <= 0x0C4D) ||
  (codepoint >= 0x0C55 && codepoint <= 0x0C56) ||
  (codepoint >= 0x0C62 && codepoint <= 0x0C63) ||
  codepoint == 0x0C81 ||
  codepoint == 0x0CBC ||
  (codepoint >= 0x0CBF && codepoint <= 0x0CC0) ||
  codepoint == 0x0CC2 ||
  (codepoint >= 0x0CC6 && codepoint <= 0x0CC8) ||
  (codepoint >= 0x0CCA && codepoint <= 0x0CCD) ||
  (codepoint >= 0x0CD5 && codepoint <= 0x0CD6) ||
  (codepoint >= 0x0CE2 && codepoint <= 0x0CE3) ||
  (codepoint >= 0x0D00 && codepoint <= 0x0D01) ||
  (codepoint >= 0x0D3B && codepoint <= 0x0D3C) ||
  codepoint == 0x0D3E ||
  (codepoint >= 0x0D41 && codepoint <= 0x0D44) ||
  codepoint == 0x0D4D ||
  codepoint == 0x0D57 ||
  (codepoint >= 0x0D62 && codepoint <= 0x0D63) ||
  codepoint == 0x0D81 ||
  codepoint == 0x0DCA ||
  codepoint == 0x0DCF ||
  (codepoint >= 0x0DD2 && codepoint <= 0x0DD4) ||
  codepoint == 0x0DD6
}

///|
fn unicode_grapheme_is_extend_17_0_chunk_2(codepoint : Int) -> Bool {
  codepoint == 0x0DDF ||
  codepoint == 0x0E31 ||
  (codepoint >= 0x0E34 && codepoint <= 0x0E3A) ||
  (codepoint >= 0x0E47 && codepoint <= 0x0E4E) ||
  codepoint == 0x0EB1 ||
  (codepoint >= 0x0EB4 && codepoint <= 0x0EBC) ||
  (codepoint >= 0x0EC8 && codepoint <= 0x0ECE) ||
  (codepoint >= 0x0F18 && codepoint <= 0x0F19) ||
  codepoint == 0x0F35 ||
  codepoint == 0x0F37 ||
  codepoint == 0x0F39 ||
  (codepoint >= 0x0F71 && codepoint <= 0x0F7E) ||
  (codepoint >= 0x0F80 && codepoint <= 0x0F84) ||
  (codepoint >= 0x0F86 && codepoint <= 0x0F87) ||
  (codepoint >= 0x0F8D && codepoint <= 0x0F97) ||
  (codepoint >= 0x0F99 && codepoint <= 0x0FBC) ||
  codepoint == 0x0FC6 ||
  (codepoint >= 0x102D && codepoint <= 0x1030) ||
  (codepoint >= 0x1032 && codepoint <= 0x1037) ||
  (codepoint >= 0x1039 && codepoint <= 0x103A) ||
  (codepoint >= 0x103D && codepoint <= 0x103E) ||
  (codepoint >= 0x1058 && codepoint <= 0x1059) ||
  (codepoint >= 0x105E && codepoint <= 0x1060) ||
  (codepoint >= 0x1071 && codepoint <= 0x1074) ||
  codepoint == 0x1082 ||
  (codepoint >= 0x1085 && codepoint <= 0x1086) ||
  codepoint == 0x108D ||
  codepoint == 0x109D ||
  (codepoint >= 0x135D && codepoint <= 0x135F) ||
  (codepoint >= 0x1712 && codepoint <= 0x1715) ||
  (codepoint >= 0x1732 && codepoint <= 0x1734) ||
  (codepoint >= 0x1752 && codepoint <= 0x1753) ||
  (codepoint >= 0x1772 && codepoint <= 0x1773) ||
  (codepoint >= 0x17B4 && codepoint <= 0x17B5) ||
  (codepoint >= 0x17B7 && codepoint <= 0x17BD) ||
  codepoint == 0x17C6 ||
  (codepoint >= 0x17C9 && codepoint <= 0x17D3) ||
  codepoint == 0x17DD ||
  (codepoint >= 0x180B && codepoint <= 0x180D) ||
  codepoint == 0x180F ||
  (codepoint >= 0x1885 && codepoint <= 0x1886) ||
  codepoint == 0x18A9 ||
  (codepoint >= 0x1920 && codepoint <= 0x1922) ||
  (codepoint >= 0x1927 && codepoint <= 0x1928) ||
  codepoint == 0x1932 ||
  (codepoint >= 0x1939 && codepoint <= 0x193B) ||
  (codepoint >= 0x1A17 && codepoint <= 0x1A18) ||
  codepoint == 0x1A1B
}

///|
fn unicode_grapheme_is_extend_17_0_chunk_3(codepoint : Int) -> Bool {
  codepoint == 0x1A56 ||
  (codepoint >= 0x1A58 && codepoint <= 0x1A5E) ||
  codepoint == 0x1A60 ||
  codepoint == 0x1A62 ||
  (codepoint >= 0x1A65 && codepoint <= 0x1A6C) ||
  (codepoint >= 0x1A73 && codepoint <= 0x1A7C) ||
  codepoint == 0x1A7F ||
  (codepoint >= 0x1AB0 && codepoint <= 0x1ADD) ||
  (codepoint >= 0x1AE0 && codepoint <= 0x1AEB) ||
  (codepoint >= 0x1B00 && codepoint <= 0x1B03) ||
  (codepoint >= 0x1B34 && codepoint <= 0x1B3D) ||
  (codepoint >= 0x1B42 && codepoint <= 0x1B44) ||
  (codepoint >= 0x1B6B && codepoint <= 0x1B73) ||
  (codepoint >= 0x1B80 && codepoint <= 0x1B81) ||
  (codepoint >= 0x1BA2 && codepoint <= 0x1BA5) ||
  (codepoint >= 0x1BA8 && codepoint <= 0x1BAD) ||
  codepoint == 0x1BE6 ||
  (codepoint >= 0x1BE8 && codepoint <= 0x1BE9) ||
  codepoint == 0x1BED ||
  (codepoint >= 0x1BEF && codepoint <= 0x1BF3) ||
  (codepoint >= 0x1C2C && codepoint <= 0x1C33) ||
  (codepoint >= 0x1C36 && codepoint <= 0x1C37) ||
  (codepoint >= 0x1CD0 && codepoint <= 0x1CD2) ||
  (codepoint >= 0x1CD4 && codepoint <= 0x1CE0) ||
  (codepoint >= 0x1CE2 && codepoint <= 0x1CE8) ||
  codepoint == 0x1CED ||
  codepoint == 0x1CF4 ||
  (codepoint >= 0x1CF8 && codepoint <= 0x1CF9) ||
  (codepoint >= 0x1DC0 && codepoint <= 0x1DFF) ||
  codepoint == 0x200C ||
  (codepoint >= 0x20D0 && codepoint <= 0x20F0) ||
  (codepoint >= 0x2CEF && codepoint <= 0x2CF1) ||
  codepoint == 0x2D7F ||
  (codepoint >= 0x2DE0 && codepoint <= 0x2DFF) ||
  (codepoint >= 0x302A && codepoint <= 0x302F) ||
  (codepoint >= 0x3099 && codepoint <= 0x309A) ||
  (codepoint >= 0xA66F && codepoint <= 0xA672) ||
  (codepoint >= 0xA674 && codepoint <= 0xA67D) ||
  (codepoint >= 0xA69E && codepoint <= 0xA69F) ||
  (codepoint >= 0xA6F0 && codepoint <= 0xA6F1) ||
  codepoint == 0xA802 ||
  codepoint == 0xA806 ||
  codepoint == 0xA80B ||
  (codepoint >= 0xA825 && codepoint <= 0xA826) ||
  codepoint == 0xA82C ||
  (codepoint >= 0xA8C4 && codepoint <= 0xA8C5) ||
  (codepoint >= 0xA8E0 && codepoint <= 0xA8F1) ||
  codepoint == 0xA8FF
}

///|
fn unicode_grapheme_is_extend_17_0_chunk_4(codepoint : Int) -> Bool {
  (codepoint >= 0xA926 && codepoint <= 0xA92D) ||
  (codepoint >= 0xA947 && codepoint <= 0xA951) ||
  codepoint == 0xA953 ||
  (codepoint >= 0xA980 && codepoint <= 0xA982) ||
  codepoint == 0xA9B3 ||
  (codepoint >= 0xA9B6 && codepoint <= 0xA9B9) ||
  (codepoint >= 0xA9BC && codepoint <= 0xA9BD) ||
  codepoint == 0xA9C0 ||
  codepoint == 0xA9E5 ||
  (codepoint >= 0xAA29 && codepoint <= 0xAA2E) ||
  (codepoint >= 0xAA31 && codepoint <= 0xAA32) ||
  (codepoint >= 0xAA35 && codepoint <= 0xAA36) ||
  codepoint == 0xAA43 ||
  codepoint == 0xAA4C ||
  codepoint == 0xAA7C ||
  codepoint == 0xAAB0 ||
  (codepoint >= 0xAAB2 && codepoint <= 0xAAB4) ||
  (codepoint >= 0xAAB7 && codepoint <= 0xAAB8) ||
  (codepoint >= 0xAABE && codepoint <= 0xAABF) ||
  codepoint == 0xAAC1 ||
  (codepoint >= 0xAAEC && codepoint <= 0xAAED) ||
  codepoint == 0xAAF6 ||
  codepoint == 0xABE5 ||
  codepoint == 0xABE8 ||
  codepoint == 0xABED ||
  codepoint == 0xFB1E ||
  (codepoint >= 0xFE00 && codepoint <= 0xFE0F) ||
  (codepoint >= 0xFE20 && codepoint <= 0xFE2F) ||
  (codepoint >= 0xFF9E && codepoint <= 0xFF9F) ||
  codepoint == 0x101FD ||
  codepoint == 0x102E0 ||
  (codepoint >= 0x10376 && codepoint <= 0x1037A) ||
  (codepoint >= 0x10A01 && codepoint <= 0x10A03) ||
  (codepoint >= 0x10A05 && codepoint <= 0x10A06) ||
  (codepoint >= 0x10A0C && codepoint <= 0x10A0F) ||
  (codepoint >= 0x10A38 && codepoint <= 0x10A3A) ||
  codepoint == 0x10A3F ||
  (codepoint >= 0x10AE5 && codepoint <= 0x10AE6) ||
  (codepoint >= 0x10D24 && codepoint <= 0x10D27) ||
  (codepoint >= 0x10D69 && codepoint <= 0x10D6D) ||
  (codepoint >= 0x10EAB && codepoint <= 0x10EAC) ||
  (codepoint >= 0x10EFA && codepoint <= 0x10EFF) ||
  (codepoint >= 0x10F46 && codepoint <= 0x10F50) ||
  (codepoint >= 0x10F82 && codepoint <= 0x10F85) ||
  codepoint == 0x11001 ||
  (codepoint >= 0x11038 && codepoint <= 0x11046) ||
  codepoint == 0x11070 ||
  (codepoint >= 0x11073 && codepoint <= 0x11074)
}

///|
fn unicode_grapheme_is_extend_17_0_chunk_5(codepoint : Int) -> Bool {
  (codepoint >= 0x1107F && codepoint <= 0x11081) ||
  (codepoint >= 0x110B3 && codepoint <= 0x110B6) ||
  (codepoint >= 0x110B9 && codepoint <= 0x110BA) ||
  codepoint == 0x110C2 ||
  (codepoint >= 0x11100 && codepoint <= 0x11102) ||
  (codepoint >= 0x11127 && codepoint <= 0x1112B) ||
  (codepoint >= 0x1112D && codepoint <= 0x11134) ||
  codepoint == 0x11173 ||
  (codepoint >= 0x11180 && codepoint <= 0x11181) ||
  (codepoint >= 0x111B6 && codepoint <= 0x111BE) ||
  codepoint == 0x111C0 ||
  (codepoint >= 0x111C9 && codepoint <= 0x111CC) ||
  codepoint == 0x111CF ||
  (codepoint >= 0x1122F && codepoint <= 0x11231) ||
  (codepoint >= 0x11234 && codepoint <= 0x11237) ||
  codepoint == 0x1123E ||
  codepoint == 0x11241 ||
  codepoint == 0x112DF ||
  (codepoint >= 0x112E3 && codepoint <= 0x112EA) ||
  (codepoint >= 0x11300 && codepoint <= 0x11301) ||
  (codepoint >= 0x1133B && codepoint <= 0x1133C) ||
  codepoint == 0x1133E ||
  codepoint == 0x11340 ||
  codepoint == 0x1134D ||
  codepoint == 0x11357 ||
  (codepoint >= 0x11366 && codepoint <= 0x1136C) ||
  (codepoint >= 0x11370 && codepoint <= 0x11374) ||
  codepoint == 0x113B8 ||
  (codepoint >= 0x113BB && codepoint <= 0x113C0) ||
  codepoint == 0x113C2 ||
  codepoint == 0x113C5 ||
  (codepoint >= 0x113C7 && codepoint <= 0x113C9) ||
  (codepoint >= 0x113CE && codepoint <= 0x113D0) ||
  codepoint == 0x113D2 ||
  (codepoint >= 0x113E1 && codepoint <= 0x113E2) ||
  (codepoint >= 0x11438 && codepoint <= 0x1143F) ||
  (codepoint >= 0x11442 && codepoint <= 0x11444) ||
  codepoint == 0x11446 ||
  codepoint == 0x1145E ||
  codepoint == 0x114B0 ||
  (codepoint >= 0x114B3 && codepoint <= 0x114B8) ||
  codepoint == 0x114BA ||
  codepoint == 0x114BD ||
  (codepoint >= 0x114BF && codepoint <= 0x114C0) ||
  (codepoint >= 0x114C2 && codepoint <= 0x114C3) ||
  codepoint == 0x115AF ||
  (codepoint >= 0x115B2 && codepoint <= 0x115B5) ||
  (codepoint >= 0x115BC && codepoint <= 0x115BD)
}

///|
fn unicode_grapheme_is_extend_17_0_chunk_6(codepoint : Int) -> Bool {
  (codepoint >= 0x115BF && codepoint <= 0x115C0) ||
  (codepoint >= 0x115DC && codepoint <= 0x115DD) ||
  (codepoint >= 0x11633 && codepoint <= 0x1163A) ||
  codepoint == 0x1163D ||
  (codepoint >= 0x1163F && codepoint <= 0x11640) ||
  codepoint == 0x116AB ||
  codepoint == 0x116AD ||
  (codepoint >= 0x116B0 && codepoint <= 0x116B7) ||
  codepoint == 0x1171D ||
  codepoint == 0x1171F ||
  (codepoint >= 0x11722 && codepoint <= 0x11725) ||
  (codepoint >= 0x11727 && codepoint <= 0x1172B) ||
  (codepoint >= 0x1182F && codepoint <= 0x11837) ||
  (codepoint >= 0x11839 && codepoint <= 0x1183A) ||
  codepoint == 0x11930 ||
  (codepoint >= 0x1193B && codepoint <= 0x1193E) ||
  codepoint == 0x11943 ||
  (codepoint >= 0x119D4 && codepoint <= 0x119D7) ||
  (codepoint >= 0x119DA && codepoint <= 0x119DB) ||
  codepoint == 0x119E0 ||
  (codepoint >= 0x11A01 && codepoint <= 0x11A0A) ||
  (codepoint >= 0x11A33 && codepoint <= 0x11A38) ||
  (codepoint >= 0x11A3B && codepoint <= 0x11A3E) ||
  codepoint == 0x11A47 ||
  (codepoint >= 0x11A51 && codepoint <= 0x11A56) ||
  (codepoint >= 0x11A59 && codepoint <= 0x11A5B) ||
  (codepoint >= 0x11A8A && codepoint <= 0x11A96) ||
  (codepoint >= 0x11A98 && codepoint <= 0x11A99) ||
  codepoint == 0x11B60 ||
  (codepoint >= 0x11B62 && codepoint <= 0x11B64) ||
  codepoint == 0x11B66 ||
  (codepoint >= 0x11C30 && codepoint <= 0x11C36) ||
  (codepoint >= 0x11C38 && codepoint <= 0x11C3D) ||
  codepoint == 0x11C3F ||
  (codepoint >= 0x11C92 && codepoint <= 0x11CA7) ||
  (codepoint >= 0x11CAA && codepoint <= 0x11CB0) ||
  (codepoint >= 0x11CB2 && codepoint <= 0x11CB3) ||
  (codepoint >= 0x11CB5 && codepoint <= 0x11CB6) ||
  (codepoint >= 0x11D31 && codepoint <= 0x11D36) ||
  codepoint == 0x11D3A ||
  (codepoint >= 0x11D3C && codepoint <= 0x11D3D) ||
  (codepoint >= 0x11D3F && codepoint <= 0x11D45) ||
  codepoint == 0x11D47 ||
  (codepoint >= 0x11D90 && codepoint <= 0x11D91) ||
  codepoint == 0x11D95 ||
  codepoint == 0x11D97 ||
  (codepoint >= 0x11EF3 && codepoint <= 0x11EF4) ||
  (codepoint >= 0x11F00 && codepoint <= 0x11F01)
}

///|
fn unicode_grapheme_is_extend_17_0_chunk_7(codepoint : Int) -> Bool {
  (codepoint >= 0x11F36 && codepoint <= 0x11F3A) ||
  (codepoint >= 0x11F40 && codepoint <= 0x11F42) ||
  codepoint == 0x11F5A ||
  codepoint == 0x13440 ||
  (codepoint >= 0x13447 && codepoint <= 0x13455) ||
  (codepoint >= 0x1611E && codepoint <= 0x16129) ||
  (codepoint >= 0x1612D && codepoint <= 0x1612F) ||
  (codepoint >= 0x16AF0 && codepoint <= 0x16AF4) ||
  (codepoint >= 0x16B30 && codepoint <= 0x16B36) ||
  codepoint == 0x16F4F ||
  (codepoint >= 0x16F8F && codepoint <= 0x16F92) ||
  codepoint == 0x16FE4 ||
  (codepoint >= 0x16FF0 && codepoint <= 0x16FF1) ||
  (codepoint >= 0x1BC9D && codepoint <= 0x1BC9E) ||
  (codepoint >= 0x1CF00 && codepoint <= 0x1CF2D) ||
  (codepoint >= 0x1CF30 && codepoint <= 0x1CF46) ||
  (codepoint >= 0x1D165 && codepoint <= 0x1D169) ||
  (codepoint >= 0x1D16D && codepoint <= 0x1D172) ||
  (codepoint >= 0x1D17B && codepoint <= 0x1D182) ||
  (codepoint >= 0x1D185 && codepoint <= 0x1D18B) ||
  (codepoint >= 0x1D1AA && codepoint <= 0x1D1AD) ||
  (codepoint >= 0x1D242 && codepoint <= 0x1D244) ||
  (codepoint >= 0x1DA00 && codepoint <= 0x1DA36) ||
  (codepoint >= 0x1DA3B && codepoint <= 0x1DA6C) ||
  codepoint == 0x1DA75 ||
  codepoint == 0x1DA84 ||
  (codepoint >= 0x1DA9B && codepoint <= 0x1DA9F) ||
  (codepoint >= 0x1DAA1 && codepoint <= 0x1DAAF) ||
  (codepoint >= 0x1E000 && codepoint <= 0x1E006) ||
  (codepoint >= 0x1E008 && codepoint <= 0x1E018) ||
  (codepoint >= 0x1E01B && codepoint <= 0x1E021) ||
  (codepoint >= 0x1E023 && codepoint <= 0x1E024) ||
  (codepoint >= 0x1E026 && codepoint <= 0x1E02A) ||
  codepoint == 0x1E08F ||
  (codepoint >= 0x1E130 && codepoint <= 0x1E136) ||
  codepoint == 0x1E2AE ||
  (codepoint >= 0x1E2EC && codepoint <= 0x1E2EF) ||
  (codepoint >= 0x1E4EC && codepoint <= 0x1E4EF) ||
  (codepoint >= 0x1E5EE && codepoint <= 0x1E5EF) ||
  codepoint == 0x1E6E3 ||
  codepoint == 0x1E6E6 ||
  (codepoint >= 0x1E6EE && codepoint <= 0x1E6EF) ||
  codepoint == 0x1E6F5 ||
  (codepoint >= 0x1E8D0 && codepoint <= 0x1E8D6) ||
  (codepoint >= 0x1E944 && codepoint <= 0x1E94A) ||
  (codepoint >= 0x1F3FB && codepoint <= 0x1F3FF) ||
  (codepoint >= 0xE0020 && codepoint <= 0xE007F) ||
  (codepoint >= 0xE0100 && codepoint <= 0xE01EF)
}

///|
fn unicode_grapheme_is_spacing_mark_17_0(codepoint : Int) -> Bool {
  unicode_grapheme_is_spacing_mark_17_0_chunk_0(codepoint) ||
  unicode_grapheme_is_spacing_mark_17_0_chunk_1(codepoint) ||
  unicode_grapheme_is_spacing_mark_17_0_chunk_2(codepoint) ||
  unicode_grapheme_is_spacing_mark_17_0_chunk_3(codepoint)
}

///|
fn unicode_grapheme_is_spacing_mark_17_0_chunk_0(codepoint : Int) -> Bool {
  codepoint == 0x0903 ||
  codepoint == 0x093B ||
  (codepoint >= 0x093E && codepoint <= 0x0940) ||
  (codepoint >= 0x0949 && codepoint <= 0x094C) ||
  (codepoint >= 0x094E && codepoint <= 0x094F) ||
  (codepoint >= 0x0982 && codepoint <= 0x0983) ||
  (codepoint >= 0x09BF && codepoint <= 0x09C0) ||
  (codepoint >= 0x09C7 && codepoint <= 0x09C8) ||
  (codepoint >= 0x09CB && codepoint <= 0x09CC) ||
  codepoint == 0x0A03 ||
  (codepoint >= 0x0A3E && codepoint <= 0x0A40) ||
  codepoint == 0x0A83 ||
  (codepoint >= 0x0ABE && codepoint <= 0x0AC0) ||
  codepoint == 0x0AC9 ||
  (codepoint >= 0x0ACB && codepoint <= 0x0ACC) ||
  (codepoint >= 0x0B02 && codepoint <= 0x0B03) ||
  codepoint == 0x0B40 ||
  (codepoint >= 0x0B47 && codepoint <= 0x0B48) ||
  (codepoint >= 0x0B4B && codepoint <= 0x0B4C) ||
  codepoint == 0x0BBF ||
  (codepoint >= 0x0BC1 && codepoint <= 0x0BC2) ||
  (codepoint >= 0x0BC6 && codepoint <= 0x0BC8) ||
  (codepoint >= 0x0BCA && codepoint <= 0x0BCC) ||
  (codepoint >= 0x0C01 && codepoint <= 0x0C03) ||
  (codepoint >= 0x0C41 && codepoint <= 0x0C44) ||
  (codepoint >= 0x0C82 && codepoint <= 0x0C83) ||
  codepoint == 0x0CBE ||
  codepoint == 0x0CC1 ||
  (codepoint >= 0x0CC3 && codepoint <= 0x0CC4) ||
  codepoint == 0x0CF3 ||
  (codepoint >= 0x0D02 && codepoint <= 0x0D03) ||
  (codepoint >= 0x0D3F && codepoint <= 0x0D40) ||
  (codepoint >= 0x0D46 && codepoint <= 0x0D48) ||
  (codepoint >= 0x0D4A && codepoint <= 0x0D4C) ||
  (codepoint >= 0x0D82 && codepoint <= 0x0D83) ||
  (codepoint >= 0x0DD0 && codepoint <= 0x0DD1) ||
  (codepoint >= 0x0DD8 && codepoint <= 0x0DDE) ||
  (codepoint >= 0x0DF2 && codepoint <= 0x0DF3) ||
  codepoint == 0x0E33 ||
  codepoint == 0x0EB3 ||
  (codepoint >= 0x0F3E && codepoint <= 0x0F3F) ||
  codepoint == 0x0F7F ||
  codepoint == 0x1031 ||
  (codepoint >= 0x103B && codepoint <= 0x103C) ||
  (codepoint >= 0x1056 && codepoint <= 0x1057) ||
  codepoint == 0x1084 ||
  codepoint == 0x17B6 ||
  (codepoint >= 0x17BE && codepoint <= 0x17C5)
}

///|
fn unicode_grapheme_is_spacing_mark_17_0_chunk_1(codepoint : Int) -> Bool {
  (codepoint >= 0x17C7 && codepoint <= 0x17C8) ||
  (codepoint >= 0x1923 && codepoint <= 0x1926) ||
  (codepoint >= 0x1929 && codepoint <= 0x192B) ||
  (codepoint >= 0x1930 && codepoint <= 0x1931) ||
  (codepoint >= 0x1933 && codepoint <= 0x1938) ||
  (codepoint >= 0x1A19 && codepoint <= 0x1A1A) ||
  codepoint == 0x1A55 ||
  codepoint == 0x1A57 ||
  (codepoint >= 0x1A6D && codepoint <= 0x1A72) ||
  codepoint == 0x1B04 ||
  (codepoint >= 0x1B3E && codepoint <= 0x1B41) ||
  codepoint == 0x1B82 ||
  codepoint == 0x1BA1 ||
  (codepoint >= 0x1BA6 && codepoint <= 0x1BA7) ||
  codepoint == 0x1BE7 ||
  (codepoint >= 0x1BEA && codepoint <= 0x1BEC) ||
  codepoint == 0x1BEE ||
  (codepoint >= 0x1C24 && codepoint <= 0x1C2B) ||
  (codepoint >= 0x1C34 && codepoint <= 0x1C35) ||
  codepoint == 0x1CE1 ||
  codepoint == 0x1CF7 ||
  (codepoint >= 0xA823 && codepoint <= 0xA824) ||
  codepoint == 0xA827 ||
  (codepoint >= 0xA880 && codepoint <= 0xA881) ||
  (codepoint >= 0xA8B4 && codepoint <= 0xA8C3) ||
  codepoint == 0xA952 ||
  codepoint == 0xA983 ||
  (codepoint >= 0xA9B4 && codepoint <= 0xA9B5) ||
  (codepoint >= 0xA9BA && codepoint <= 0xA9BB) ||
  (codepoint >= 0xA9BE && codepoint <= 0xA9BF) ||
  (codepoint >= 0xAA2F && codepoint <= 0xAA30) ||
  (codepoint >= 0xAA33 && codepoint <= 0xAA34) ||
  codepoint == 0xAA4D ||
  codepoint == 0xAAEB ||
  (codepoint >= 0xAAEE && codepoint <= 0xAAEF) ||
  codepoint == 0xAAF5 ||
  (codepoint >= 0xABE3 && codepoint <= 0xABE4) ||
  (codepoint >= 0xABE6 && codepoint <= 0xABE7) ||
  (codepoint >= 0xABE9 && codepoint <= 0xABEA) ||
  codepoint == 0xABEC ||
  codepoint == 0x11000 ||
  codepoint == 0x11002 ||
  codepoint == 0x11082 ||
  (codepoint >= 0x110B0 && codepoint <= 0x110B2) ||
  (codepoint >= 0x110B7 && codepoint <= 0x110B8) ||
  codepoint == 0x1112C ||
  (codepoint >= 0x11145 && codepoint <= 0x11146) ||
  codepoint == 0x11182
}

///|
fn unicode_grapheme_is_spacing_mark_17_0_chunk_2(codepoint : Int) -> Bool {
  (codepoint >= 0x111B3 && codepoint <= 0x111B5) ||
  codepoint == 0x111BF ||
  codepoint == 0x111CE ||
  (codepoint >= 0x1122C && codepoint <= 0x1122E) ||
  (codepoint >= 0x11232 && codepoint <= 0x11233) ||
  (codepoint >= 0x112E0 && codepoint <= 0x112E2) ||
  (codepoint >= 0x11302 && codepoint <= 0x11303) ||
  codepoint == 0x1133F ||
  (codepoint >= 0x11341 && codepoint <= 0x11344) ||
  (codepoint >= 0x11347 && codepoint <= 0x11348) ||
  (codepoint >= 0x1134B && codepoint <= 0x1134C) ||
  (codepoint >= 0x11362 && codepoint <= 0x11363) ||
  (codepoint >= 0x113B9 && codepoint <= 0x113BA) ||
  codepoint == 0x113CA ||
  (codepoint >= 0x113CC && codepoint <= 0x113CD) ||
  (codepoint >= 0x11435 && codepoint <= 0x11437) ||
  (codepoint >= 0x11440 && codepoint <= 0x11441) ||
  codepoint == 0x11445 ||
  (codepoint >= 0x114B1 && codepoint <= 0x114B2) ||
  codepoint == 0x114B9 ||
  (codepoint >= 0x114BB && codepoint <= 0x114BC) ||
  codepoint == 0x114BE ||
  codepoint == 0x114C1 ||
  (codepoint >= 0x115B0 && codepoint <= 0x115B1) ||
  (codepoint >= 0x115B8 && codepoint <= 0x115BB) ||
  codepoint == 0x115BE ||
  (codepoint >= 0x11630 && codepoint <= 0x11632) ||
  (codepoint >= 0x1163B && codepoint <= 0x1163C) ||
  codepoint == 0x1163E ||
  codepoint == 0x116AC ||
  (codepoint >= 0x116AE && codepoint <= 0x116AF) ||
  codepoint == 0x1171E ||
  codepoint == 0x11726 ||
  (codepoint >= 0x1182C && codepoint <= 0x1182E) ||
  codepoint == 0x11838 ||
  (codepoint >= 0x11931 && codepoint <= 0x11935) ||
  (codepoint >= 0x11937 && codepoint <= 0x11938) ||
  codepoint == 0x11940 ||
  codepoint == 0x11942 ||
  (codepoint >= 0x119D1 && codepoint <= 0x119D3) ||
  (codepoint >= 0x119DC && codepoint <= 0x119DF) ||
  codepoint == 0x119E4 ||
  codepoint == 0x11A39 ||
  (codepoint >= 0x11A57 && codepoint <= 0x11A58) ||
  codepoint == 0x11A97 ||
  codepoint == 0x11B61 ||
  codepoint == 0x11B65 ||
  codepoint == 0x11B67
}

///|
fn unicode_grapheme_is_spacing_mark_17_0_chunk_3(codepoint : Int) -> Bool {
  codepoint == 0x11C2F ||
  codepoint == 0x11C3E ||
  codepoint == 0x11CA9 ||
  codepoint == 0x11CB1 ||
  codepoint == 0x11CB4 ||
  (codepoint >= 0x11D8A && codepoint <= 0x11D8E) ||
  (codepoint >= 0x11D93 && codepoint <= 0x11D94) ||
  codepoint == 0x11D96 ||
  (codepoint >= 0x11EF5 && codepoint <= 0x11EF6) ||
  codepoint == 0x11F03 ||
  (codepoint >= 0x11F34 && codepoint <= 0x11F35) ||
  (codepoint >= 0x11F3E && codepoint <= 0x11F3F) ||
  (codepoint >= 0x1612A && codepoint <= 0x1612C) ||
  (codepoint >= 0x16F51 && codepoint <= 0x16F87)
}

///|
fn unicode_grapheme_is_regional_indicator_17_0(codepoint : Int) -> Bool {
  codepoint >= 0x1F1E6 && codepoint <= 0x1F1FF
}

///|
fn unicode_grapheme_is_zwj_17_0(codepoint : Int) -> Bool {
  codepoint == 0x200D
}

///|
fn unicode_emoji_is_extended_pictographic_17_0(codepoint : Int) -> Bool {
  unicode_emoji_is_extended_pictographic_17_0_chunk_0(codepoint) ||
  unicode_emoji_is_extended_pictographic_17_0_chunk_1(codepoint) ||
  unicode_emoji_is_extended_pictographic_17_0_chunk_2(codepoint) ||
  unicode_emoji_is_extended_pictographic_17_0_chunk_3(codepoint)
}

///|
fn unicode_emoji_is_extended_pictographic_17_0_chunk_0(codepoint : Int) -> Bool {
  codepoint == 0x00A9 ||
  codepoint == 0x00AE ||
  codepoint == 0x203C ||
  codepoint == 0x2049 ||
  codepoint == 0x2122 ||
  codepoint == 0x2139 ||
  (codepoint >= 0x2194 && codepoint <= 0x2199) ||
  (codepoint >= 0x21A9 && codepoint <= 0x21AA) ||
  (codepoint >= 0x231A && codepoint <= 0x231B) ||
  codepoint == 0x2328 ||
  codepoint == 0x23CF ||
  (codepoint >= 0x23E9 && codepoint <= 0x23F3) ||
  (codepoint >= 0x23F8 && codepoint <= 0x23FA) ||
  codepoint == 0x24C2 ||
  (codepoint >= 0x25AA && codepoint <= 0x25AB) ||
  codepoint == 0x25B6 ||
  codepoint == 0x25C0 ||
  (codepoint >= 0x25FB && codepoint <= 0x25FE) ||
  (codepoint >= 0x2600 && codepoint <= 0x2604) ||
  codepoint == 0x260E ||
  codepoint == 0x2611 ||
  (codepoint >= 0x2614 && codepoint <= 0x2615) ||
  codepoint == 0x2618 ||
  codepoint == 0x261D ||
  codepoint == 0x2620 ||
  (codepoint >= 0x2622 && codepoint <= 0x2623) ||
  codepoint == 0x2626 ||
  codepoint == 0x262A ||
  (codepoint >= 0x262E && codepoint <= 0x262F) ||
  (codepoint >= 0x2638 && codepoint <= 0x263A) ||
  codepoint == 0x2640 ||
  codepoint == 0x2642 ||
  (codepoint >= 0x2648 && codepoint <= 0x2653) ||
  (codepoint >= 0x265F && codepoint <= 0x2660) ||
  codepoint == 0x2663 ||
  (codepoint >= 0x2665 && codepoint <= 0x2666) ||
  codepoint == 0x2668 ||
  codepoint == 0x267B ||
  (codepoint >= 0x267E && codepoint <= 0x267F) ||
  (codepoint >= 0x2692 && codepoint <= 0x2697) ||
  codepoint == 0x2699 ||
  (codepoint >= 0x269B && codepoint <= 0x269C) ||
  (codepoint >= 0x26A0 && codepoint <= 0x26A1) ||
  codepoint == 0x26A7 ||
  (codepoint >= 0x26AA && codepoint <= 0x26AB) ||
  (codepoint >= 0x26B0 && codepoint <= 0x26B1) ||
  (codepoint >= 0x26BD && codepoint <= 0x26BE) ||
  (codepoint >= 0x26C4 && codepoint <= 0x26C5)
}

///|
fn unicode_emoji_is_extended_pictographic_17_0_chunk_1(codepoint : Int) -> Bool {
  codepoint == 0x26C8 ||
  (codepoint >= 0x26CE && codepoint <= 0x26CF) ||
  codepoint == 0x26D1 ||
  (codepoint >= 0x26D3 && codepoint <= 0x26D4) ||
  (codepoint >= 0x26E9 && codepoint <= 0x26EA) ||
  (codepoint >= 0x26F0 && codepoint <= 0x26F5) ||
  (codepoint >= 0x26F7 && codepoint <= 0x26FA) ||
  codepoint == 0x26FD ||
  codepoint == 0x2702 ||
  codepoint == 0x2705 ||
  (codepoint >= 0x2708 && codepoint <= 0x270D) ||
  codepoint == 0x270F ||
  codepoint == 0x2712 ||
  codepoint == 0x2714 ||
  codepoint == 0x2716 ||
  codepoint == 0x271D ||
  codepoint == 0x2721 ||
  codepoint == 0x2728 ||
  (codepoint >= 0x2733 && codepoint <= 0x2734) ||
  codepoint == 0x2744 ||
  codepoint == 0x2747 ||
  codepoint == 0x274C ||
  codepoint == 0x274E ||
  (codepoint >= 0x2753 && codepoint <= 0x2755) ||
  codepoint == 0x2757 ||
  (codepoint >= 0x2763 && codepoint <= 0x2764) ||
  (codepoint >= 0x2795 && codepoint <= 0x2797) ||
  codepoint == 0x27A1 ||
  codepoint == 0x27B0 ||
  codepoint == 0x27BF ||
  (codepoint >= 0x2934 && codepoint <= 0x2935) ||
  (codepoint >= 0x2B05 && codepoint <= 0x2B07) ||
  (codepoint >= 0x2B1B && codepoint <= 0x2B1C) ||
  codepoint == 0x2B50 ||
  codepoint == 0x2B55 ||
  codepoint == 0x3030 ||
  codepoint == 0x303D ||
  codepoint == 0x3297 ||
  codepoint == 0x3299 ||
  codepoint == 0x1F004 ||
  (codepoint >= 0x1F02C && codepoint <= 0x1F02F) ||
  (codepoint >= 0x1F094 && codepoint <= 0x1F09F) ||
  (codepoint >= 0x1F0AF && codepoint <= 0x1F0B0) ||
  codepoint == 0x1F0C0 ||
  (codepoint >= 0x1F0CF && codepoint <= 0x1F0D0) ||
  (codepoint >= 0x1F0F6 && codepoint <= 0x1F0FF) ||
  (codepoint >= 0x1F170 && codepoint <= 0x1F171) ||
  (codepoint >= 0x1F17E && codepoint <= 0x1F17F)
}

///|
fn unicode_emoji_is_extended_pictographic_17_0_chunk_2(codepoint : Int) -> Bool {
  codepoint == 0x1F18E ||
  (codepoint >= 0x1F191 && codepoint <= 0x1F19A) ||
  (codepoint >= 0x1F1AE && codepoint <= 0x1F1E5) ||
  (codepoint >= 0x1F201 && codepoint <= 0x1F20F) ||
  codepoint == 0x1F21A ||
  codepoint == 0x1F22F ||
  (codepoint >= 0x1F232 && codepoint <= 0x1F23A) ||
  (codepoint >= 0x1F23C && codepoint <= 0x1F23F) ||
  (codepoint >= 0x1F249 && codepoint <= 0x1F25F) ||
  (codepoint >= 0x1F266 && codepoint <= 0x1F321) ||
  (codepoint >= 0x1F324 && codepoint <= 0x1F393) ||
  (codepoint >= 0x1F396 && codepoint <= 0x1F397) ||
  (codepoint >= 0x1F399 && codepoint <= 0x1F39B) ||
  (codepoint >= 0x1F39E && codepoint <= 0x1F3F0) ||
  (codepoint >= 0x1F3F3 && codepoint <= 0x1F3F5) ||
  (codepoint >= 0x1F3F7 && codepoint <= 0x1F3FA) ||
  (codepoint >= 0x1F400 && codepoint <= 0x1F4FD) ||
  (codepoint >= 0x1F4FF && codepoint <= 0x1F53D) ||
  (codepoint >= 0x1F549 && codepoint <= 0x1F54E) ||
  (codepoint >= 0x1F550 && codepoint <= 0x1F567) ||
  (codepoint >= 0x1F56F && codepoint <= 0x1F570) ||
  (codepoint >= 0x1F573 && codepoint <= 0x1F57A) ||
  codepoint == 0x1F587 ||
  (codepoint >= 0x1F58A && codepoint <= 0x1F58D) ||
  codepoint == 0x1F590 ||
  (codepoint >= 0x1F595 && codepoint <= 0x1F596) ||
  (codepoint >= 0x1F5A4 && codepoint <= 0x1F5A5) ||
  codepoint == 0x1F5A8 ||
  (codepoint >= 0x1F5B1 && codepoint <= 0x1F5B2) ||
  codepoint == 0x1F5BC ||
  (codepoint >= 0x1F5C2 && codepoint <= 0x1F5C4) ||
  (codepoint >= 0x1F5D1 && codepoint <= 0x1F5D3) ||
  (codepoint >= 0x1F5DC && codepoint <= 0x1F5DE) ||
  codepoint == 0x1F5E1 ||
  codepoint == 0x1F5E3 ||
  codepoint == 0x1F5E8 ||
  codepoint == 0x1F5EF ||
  codepoint == 0x1F5F3 ||
  (codepoint >= 0x1F5FA && codepoint <= 0x1F64F) ||
  (codepoint >= 0x1F680 && codepoint <= 0x1F6C5) ||
  (codepoint >= 0x1F6CB && codepoint <= 0x1F6D2) ||
  (codepoint >= 0x1F6D5 && codepoint <= 0x1F6E5) ||
  codepoint == 0x1F6E9 ||
  (codepoint >= 0x1F6EB && codepoint <= 0x1F6F0) ||
  (codepoint >= 0x1F6F3 && codepoint <= 0x1F6FF) ||
  (codepoint >= 0x1F7DA && codepoint <= 0x1F7FF) ||
  (codepoint >= 0x1F80C && codepoint <= 0x1F80F) ||
  (codepoint >= 0x1F848 && codepoint <= 0x1F84F)
}

///|
fn unicode_emoji_is_extended_pictographic_17_0_chunk_3(codepoint : Int) -> Bool {
  (codepoint >= 0x1F85A && codepoint <= 0x1F85F) ||
  (codepoint >= 0x1F888 && codepoint <= 0x1F88F) ||
  (codepoint >= 0x1F8AE && codepoint <= 0x1F8AF) ||
  (codepoint >= 0x1F8BC && codepoint <= 0x1F8BF) ||
  (codepoint >= 0x1F8C2 && codepoint <= 0x1F8CF) ||
  (codepoint >= 0x1F8D9 && codepoint <= 0x1F8FF) ||
  (codepoint >= 0x1F90C && codepoint <= 0x1F93A) ||
  (codepoint >= 0x1F93C && codepoint <= 0x1F945) ||
  (codepoint >= 0x1F947 && codepoint <= 0x1F9FF) ||
  (codepoint >= 0x1FA58 && codepoint <= 0x1FA5F) ||
  (codepoint >= 0x1FA6E && codepoint <= 0x1FAFF) ||
  (codepoint >= 0x1FC00 && codepoint <= 0x1FFFD)
}

///|
fn unicode_indic_conjunct_is_linker_17_0(codepoint : Int) -> Bool {
  codepoint == 0x094D ||
  codepoint == 0x09CD ||
  codepoint == 0x0ACD ||
  codepoint == 0x0B4D ||
  codepoint == 0x0C4D ||
  codepoint == 0x0D4D ||
  codepoint == 0x1039 ||
  codepoint == 0x17D2 ||
  codepoint == 0x1A60 ||
  codepoint == 0x1B44 ||
  codepoint == 0x1BAB ||
  codepoint == 0xA9C0 ||
  codepoint == 0xAAF6 ||
  codepoint == 0x10A3F ||
  codepoint == 0x11133 ||
  codepoint == 0x113D0 ||
  codepoint == 0x1193E ||
  codepoint == 0x11A47 ||
  codepoint == 0x11A99 ||
  codepoint == 0x11F42
}

///|
fn unicode_indic_conjunct_is_consonant_17_0(codepoint : Int) -> Bool {
  unicode_indic_conjunct_is_consonant_17_0_chunk_0(codepoint) ||
  unicode_indic_conjunct_is_consonant_17_0_chunk_1(codepoint)
}

///|
fn unicode_indic_conjunct_is_consonant_17_0_chunk_0(codepoint : Int) -> Bool {
  (codepoint >= 0x0915 && codepoint <= 0x0939) ||
  (codepoint >= 0x0958 && codepoint <= 0x095F) ||
  (codepoint >= 0x0978 && codepoint <= 0x097F) ||
  (codepoint >= 0x0995 && codepoint <= 0x09A8) ||
  (codepoint >= 0x09AA && codepoint <= 0x09B0) ||
  codepoint == 0x09B2 ||
  (codepoint >= 0x09B6 && codepoint <= 0x09B9) ||
  (codepoint >= 0x09DC && codepoint <= 0x09DD) ||
  codepoint == 0x09DF ||
  (codepoint >= 0x09F0 && codepoint <= 0x09F1) ||
  (codepoint >= 0x0A95 && codepoint <= 0x0AA8) ||
  (codepoint >= 0x0AAA && codepoint <= 0x0AB0) ||
  (codepoint >= 0x0AB2 && codepoint <= 0x0AB3) ||
  (codepoint >= 0x0AB5 && codepoint <= 0x0AB9) ||
  codepoint == 0x0AF9 ||
  (codepoint >= 0x0B15 && codepoint <= 0x0B28) ||
  (codepoint >= 0x0B2A && codepoint <= 0x0B30) ||
  (codepoint >= 0x0B32 && codepoint <= 0x0B33) ||
  (codepoint >= 0x0B35 && codepoint <= 0x0B39) ||
  (codepoint >= 0x0B5C && codepoint <= 0x0B5D) ||
  codepoint == 0x0B5F ||
  codepoint == 0x0B71 ||
  (codepoint >= 0x0C15 && codepoint <= 0x0C28) ||
  (codepoint >= 0x0C2A && codepoint <= 0x0C39) ||
  (codepoint >= 0x0C58 && codepoint <= 0x0C5A) ||
  (codepoint >= 0x0D15 && codepoint <= 0x0D3A) ||
  (codepoint >= 0x1000 && codepoint <= 0x102A) ||
  codepoint == 0x103F ||
  (codepoint >= 0x1050 && codepoint <= 0x1055) ||
  (codepoint >= 0x105A && codepoint <= 0x105D) ||
  codepoint == 0x1061 ||
  (codepoint >= 0x1065 && codepoint <= 0x1066) ||
  (codepoint >= 0x106E && codepoint <= 0x1070) ||
  (codepoint >= 0x1075 && codepoint <= 0x1081) ||
  codepoint == 0x108E ||
  (codepoint >= 0x1780 && codepoint <= 0x17B3) ||
  (codepoint >= 0x1A20 && codepoint <= 0x1A54) ||
  (codepoint >= 0x1B0B && codepoint <= 0x1B0C) ||
  (codepoint >= 0x1B13 && codepoint <= 0x1B33) ||
  (codepoint >= 0x1B45 && codepoint <= 0x1B4C) ||
  (codepoint >= 0x1B83 && codepoint <= 0x1BA0) ||
  (codepoint >= 0x1BAE && codepoint <= 0x1BAF) ||
  (codepoint >= 0x1BBB && codepoint <= 0x1BBD) ||
  (codepoint >= 0xA989 && codepoint <= 0xA98B) ||
  (codepoint >= 0xA98F && codepoint <= 0xA9B2) ||
  (codepoint >= 0xA9E0 && codepoint <= 0xA9E4) ||
  (codepoint >= 0xA9E7 && codepoint <= 0xA9EF) ||
  (codepoint >= 0xA9FA && codepoint <= 0xA9FE)
}

///|
fn unicode_indic_conjunct_is_consonant_17_0_chunk_1(codepoint : Int) -> Bool {
  (codepoint >= 0xAA60 && codepoint <= 0xAA6F) ||
  (codepoint >= 0xAA71 && codepoint <= 0xAA73) ||
  codepoint == 0xAA7A ||
  (codepoint >= 0xAA7E && codepoint <= 0xAA7F) ||
  (codepoint >= 0xAAE0 && codepoint <= 0xAAEA) ||
  (codepoint >= 0xABC0 && codepoint <= 0xABDA) ||
  codepoint == 0x10A00 ||
  (codepoint >= 0x10A10 && codepoint <= 0x10A13) ||
  (codepoint >= 0x10A15 && codepoint <= 0x10A17) ||
  (codepoint >= 0x10A19 && codepoint <= 0x10A35) ||
  (codepoint >= 0x11103 && codepoint <= 0x11126) ||
  codepoint == 0x11144 ||
  codepoint == 0x11147 ||
  (codepoint >= 0x11380 && codepoint <= 0x11389) ||
  codepoint == 0x1138B ||
  codepoint == 0x1138E ||
  (codepoint >= 0x11390 && codepoint <= 0x113B5) ||
  (codepoint >= 0x11900 && codepoint <= 0x11906) ||
  codepoint == 0x11909 ||
  (codepoint >= 0x1190C && codepoint <= 0x11913) ||
  (codepoint >= 0x11915 && codepoint <= 0x11916) ||
  (codepoint >= 0x11918 && codepoint <= 0x1192F) ||
  codepoint == 0x11A00 ||
  (codepoint >= 0x11A0B && codepoint <= 0x11A32) ||
  codepoint == 0x11A50 ||
  (codepoint >= 0x11A5C && codepoint <= 0x11A83) ||
  (codepoint >= 0x11F04 && codepoint <= 0x11F10) ||
  (codepoint >= 0x11F12 && codepoint <= 0x11F33)
}

///|
fn unicode_indic_conjunct_is_extend_17_0(codepoint : Int) -> Bool {
  unicode_indic_conjunct_is_extend_17_0_chunk_0(codepoint) ||
  unicode_indic_conjunct_is_extend_17_0_chunk_1(codepoint) ||
  unicode_indic_conjunct_is_extend_17_0_chunk_2(codepoint) ||
  unicode_indic_conjunct_is_extend_17_0_chunk_3(codepoint) ||
  unicode_indic_conjunct_is_extend_17_0_chunk_4(codepoint) ||
  unicode_indic_conjunct_is_extend_17_0_chunk_5(codepoint) ||
  unicode_indic_conjunct_is_extend_17_0_chunk_6(codepoint) ||
  unicode_indic_conjunct_is_extend_17_0_chunk_7(codepoint)
}

///|
fn unicode_indic_conjunct_is_extend_17_0_chunk_0(codepoint : Int) -> Bool {
  (codepoint >= 0x0300 && codepoint <= 0x036F) ||
  (codepoint >= 0x0483 && codepoint <= 0x0489) ||
  (codepoint >= 0x0591 && codepoint <= 0x05BD) ||
  codepoint == 0x05BF ||
  (codepoint >= 0x05C1 && codepoint <= 0x05C2) ||
  (codepoint >= 0x05C4 && codepoint <= 0x05C5) ||
  codepoint == 0x05C7 ||
  (codepoint >= 0x0610 && codepoint <= 0x061A) ||
  (codepoint >= 0x064B && codepoint <= 0x065F) ||
  codepoint == 0x0670 ||
  (codepoint >= 0x06D6 && codepoint <= 0x06DC) ||
  (codepoint >= 0x06DF && codepoint <= 0x06E4) ||
  (codepoint >= 0x06E7 && codepoint <= 0x06E8) ||
  (codepoint >= 0x06EA && codepoint <= 0x06ED) ||
  codepoint == 0x0711 ||
  (codepoint >= 0x0730 && codepoint <= 0x074A) ||
  (codepoint >= 0x07A6 && codepoint <= 0x07B0) ||
  (codepoint >= 0x07EB && codepoint <= 0x07F3) ||
  codepoint == 0x07FD ||
  (codepoint >= 0x0816 && codepoint <= 0x0819) ||
  (codepoint >= 0x081B && codepoint <= 0x0823) ||
  (codepoint >= 0x0825 && codepoint <= 0x0827) ||
  (codepoint >= 0x0829 && codepoint <= 0x082D) ||
  (codepoint >= 0x0859 && codepoint <= 0x085B) ||
  (codepoint >= 0x0897 && codepoint <= 0x089F) ||
  (codepoint >= 0x08CA && codepoint <= 0x08E1) ||
  (codepoint >= 0x08E3 && codepoint <= 0x0902) ||
  codepoint == 0x093A ||
  codepoint == 0x093C ||
  (codepoint >= 0x0941 && codepoint <= 0x0948) ||
  (codepoint >= 0x0951 && codepoint <= 0x0957) ||
  (codepoint >= 0x0962 && codepoint <= 0x0963) ||
  codepoint == 0x0981 ||
  codepoint == 0x09BC ||
  codepoint == 0x09BE ||
  (codepoint >= 0x09C1 && codepoint <= 0x09C4) ||
  codepoint == 0x09D7 ||
  (codepoint >= 0x09E2 && codepoint <= 0x09E3) ||
  codepoint == 0x09FE ||
  (codepoint >= 0x0A01 && codepoint <= 0x0A02) ||
  codepoint == 0x0A3C ||
  (codepoint >= 0x0A41 && codepoint <= 0x0A42) ||
  (codepoint >= 0x0A47 && codepoint <= 0x0A48) ||
  (codepoint >= 0x0A4B && codepoint <= 0x0A4D) ||
  codepoint == 0x0A51 ||
  (codepoint >= 0x0A70 && codepoint <= 0x0A71) ||
  codepoint == 0x0A75 ||
  (codepoint >= 0x0A81 && codepoint <= 0x0A82)
}

///|
fn unicode_indic_conjunct_is_extend_17_0_chunk_1(codepoint : Int) -> Bool {
  codepoint == 0x0ABC ||
  (codepoint >= 0x0AC1 && codepoint <= 0x0AC5) ||
  (codepoint >= 0x0AC7 && codepoint <= 0x0AC8) ||
  (codepoint >= 0x0AE2 && codepoint <= 0x0AE3) ||
  (codepoint >= 0x0AFA && codepoint <= 0x0AFF) ||
  codepoint == 0x0B01 ||
  codepoint == 0x0B3C ||
  (codepoint >= 0x0B3E && codepoint <= 0x0B3F) ||
  (codepoint >= 0x0B41 && codepoint <= 0x0B44) ||
  (codepoint >= 0x0B55 && codepoint <= 0x0B57) ||
  (codepoint >= 0x0B62 && codepoint <= 0x0B63) ||
  codepoint == 0x0B82 ||
  codepoint == 0x0BBE ||
  codepoint == 0x0BC0 ||
  codepoint == 0x0BCD ||
  codepoint == 0x0BD7 ||
  codepoint == 0x0C00 ||
  codepoint == 0x0C04 ||
  codepoint == 0x0C3C ||
  (codepoint >= 0x0C3E && codepoint <= 0x0C40) ||
  (codepoint >= 0x0C46 && codepoint <= 0x0C48) ||
  (codepoint >= 0x0C4A && codepoint <= 0x0C4C) ||
  (codepoint >= 0x0C55 && codepoint <= 0x0C56) ||
  (codepoint >= 0x0C62 && codepoint <= 0x0C63) ||
  codepoint == 0x0C81 ||
  codepoint == 0x0CBC ||
  (codepoint >= 0x0CBF && codepoint <= 0x0CC0) ||
  codepoint == 0x0CC2 ||
  (codepoint >= 0x0CC6 && codepoint <= 0x0CC8) ||
  (codepoint >= 0x0CCA && codepoint <= 0x0CCD) ||
  (codepoint >= 0x0CD5 && codepoint <= 0x0CD6) ||
  (codepoint >= 0x0CE2 && codepoint <= 0x0CE3) ||
  (codepoint >= 0x0D00 && codepoint <= 0x0D01) ||
  (codepoint >= 0x0D3B && codepoint <= 0x0D3C) ||
  codepoint == 0x0D3E ||
  (codepoint >= 0x0D41 && codepoint <= 0x0D44) ||
  codepoint == 0x0D57 ||
  (codepoint >= 0x0D62 && codepoint <= 0x0D63) ||
  codepoint == 0x0D81 ||
  codepoint == 0x0DCA ||
  codepoint == 0x0DCF ||
  (codepoint >= 0x0DD2 && codepoint <= 0x0DD4) ||
  codepoint == 0x0DD6 ||
  codepoint == 0x0DDF ||
  codepoint == 0x0E31 ||
  (codepoint >= 0x0E34 && codepoint <= 0x0E3A) ||
  (codepoint >= 0x0E47 && codepoint <= 0x0E4E) ||
  codepoint == 0x0EB1
}

///|
fn unicode_indic_conjunct_is_extend_17_0_chunk_2(codepoint : Int) -> Bool {
  (codepoint >= 0x0EB4 && codepoint <= 0x0EBC) ||
  (codepoint >= 0x0EC8 && codepoint <= 0x0ECE) ||
  (codepoint >= 0x0F18 && codepoint <= 0x0F19) ||
  codepoint == 0x0F35 ||
  codepoint == 0x0F37 ||
  codepoint == 0x0F39 ||
  (codepoint >= 0x0F71 && codepoint <= 0x0F7E) ||
  (codepoint >= 0x0F80 && codepoint <= 0x0F84) ||
  (codepoint >= 0x0F86 && codepoint <= 0x0F87) ||
  (codepoint >= 0x0F8D && codepoint <= 0x0F97) ||
  (codepoint >= 0x0F99 && codepoint <= 0x0FBC) ||
  codepoint == 0x0FC6 ||
  (codepoint >= 0x102D && codepoint <= 0x1030) ||
  (codepoint >= 0x1032 && codepoint <= 0x1037) ||
  codepoint == 0x103A ||
  (codepoint >= 0x103D && codepoint <= 0x103E) ||
  (codepoint >= 0x1058 && codepoint <= 0x1059) ||
  (codepoint >= 0x105E && codepoint <= 0x1060) ||
  (codepoint >= 0x1071 && codepoint <= 0x1074) ||
  codepoint == 0x1082 ||
  (codepoint >= 0x1085 && codepoint <= 0x1086) ||
  codepoint == 0x108D ||
  codepoint == 0x109D ||
  (codepoint >= 0x135D && codepoint <= 0x135F) ||
  (codepoint >= 0x1712 && codepoint <= 0x1715) ||
  (codepoint >= 0x1732 && codepoint <= 0x1734) ||
  (codepoint >= 0x1752 && codepoint <= 0x1753) ||
  (codepoint >= 0x1772 && codepoint <= 0x1773) ||
  (codepoint >= 0x17B4 && codepoint <= 0x17B5) ||
  (codepoint >= 0x17B7 && codepoint <= 0x17BD) ||
  codepoint == 0x17C6 ||
  (codepoint >= 0x17C9 && codepoint <= 0x17D1) ||
  codepoint == 0x17D3 ||
  codepoint == 0x17DD ||
  (codepoint >= 0x180B && codepoint <= 0x180D) ||
  codepoint == 0x180F ||
  (codepoint >= 0x1885 && codepoint <= 0x1886) ||
  codepoint == 0x18A9 ||
  (codepoint >= 0x1920 && codepoint <= 0x1922) ||
  (codepoint >= 0x1927 && codepoint <= 0x1928) ||
  codepoint == 0x1932 ||
  (codepoint >= 0x1939 && codepoint <= 0x193B) ||
  (codepoint >= 0x1A17 && codepoint <= 0x1A18) ||
  codepoint == 0x1A1B ||
  codepoint == 0x1A56 ||
  (codepoint >= 0x1A58 && codepoint <= 0x1A5E) ||
  codepoint == 0x1A62 ||
  (codepoint >= 0x1A65 && codepoint <= 0x1A6C)
}

///|
fn unicode_indic_conjunct_is_extend_17_0_chunk_3(codepoint : Int) -> Bool {
  (codepoint >= 0x1A73 && codepoint <= 0x1A7C) ||
  codepoint == 0x1A7F ||
  (codepoint >= 0x1AB0 && codepoint <= 0x1ADD) ||
  (codepoint >= 0x1AE0 && codepoint <= 0x1AEB) ||
  (codepoint >= 0x1B00 && codepoint <= 0x1B03) ||
  (codepoint >= 0x1B34 && codepoint <= 0x1B3D) ||
  (codepoint >= 0x1B42 && codepoint <= 0x1B43) ||
  (codepoint >= 0x1B6B && codepoint <= 0x1B73) ||
  (codepoint >= 0x1B80 && codepoint <= 0x1B81) ||
  (codepoint >= 0x1BA2 && codepoint <= 0x1BA5) ||
  (codepoint >= 0x1BA8 && codepoint <= 0x1BAA) ||
  (codepoint >= 0x1BAC && codepoint <= 0x1BAD) ||
  codepoint == 0x1BE6 ||
  (codepoint >= 0x1BE8 && codepoint <= 0x1BE9) ||
  codepoint == 0x1BED ||
  (codepoint >= 0x1BEF && codepoint <= 0x1BF3) ||
  (codepoint >= 0x1C2C && codepoint <= 0x1C33) ||
  (codepoint >= 0x1C36 && codepoint <= 0x1C37) ||
  (codepoint >= 0x1CD0 && codepoint <= 0x1CD2) ||
  (codepoint >= 0x1CD4 && codepoint <= 0x1CE0) ||
  (codepoint >= 0x1CE2 && codepoint <= 0x1CE8) ||
  codepoint == 0x1CED ||
  codepoint == 0x1CF4 ||
  (codepoint >= 0x1CF8 && codepoint <= 0x1CF9) ||
  (codepoint >= 0x1DC0 && codepoint <= 0x1DFF) ||
  codepoint == 0x200D ||
  (codepoint >= 0x20D0 && codepoint <= 0x20F0) ||
  (codepoint >= 0x2CEF && codepoint <= 0x2CF1) ||
  codepoint == 0x2D7F ||
  (codepoint >= 0x2DE0 && codepoint <= 0x2DFF) ||
  (codepoint >= 0x302A && codepoint <= 0x302F) ||
  (codepoint >= 0x3099 && codepoint <= 0x309A) ||
  (codepoint >= 0xA66F && codepoint <= 0xA672) ||
  (codepoint >= 0xA674 && codepoint <= 0xA67D) ||
  (codepoint >= 0xA69E && codepoint <= 0xA69F) ||
  (codepoint >= 0xA6F0 && codepoint <= 0xA6F1) ||
  codepoint == 0xA802 ||
  codepoint == 0xA806 ||
  codepoint == 0xA80B ||
  (codepoint >= 0xA825 && codepoint <= 0xA826) ||
  codepoint == 0xA82C ||
  (codepoint >= 0xA8C4 && codepoint <= 0xA8C5) ||
  (codepoint >= 0xA8E0 && codepoint <= 0xA8F1) ||
  codepoint == 0xA8FF ||
  (codepoint >= 0xA926 && codepoint <= 0xA92D) ||
  (codepoint >= 0xA947 && codepoint <= 0xA951) ||
  codepoint == 0xA953 ||
  (codepoint >= 0xA980 && codepoint <= 0xA982)
}

///|
fn unicode_indic_conjunct_is_extend_17_0_chunk_4(codepoint : Int) -> Bool {
  codepoint == 0xA9B3 ||
  (codepoint >= 0xA9B6 && codepoint <= 0xA9B9) ||
  (codepoint >= 0xA9BC && codepoint <= 0xA9BD) ||
  codepoint == 0xA9E5 ||
  (codepoint >= 0xAA29 && codepoint <= 0xAA2E) ||
  (codepoint >= 0xAA31 && codepoint <= 0xAA32) ||
  (codepoint >= 0xAA35 && codepoint <= 0xAA36) ||
  codepoint == 0xAA43 ||
  codepoint == 0xAA4C ||
  codepoint == 0xAA7C ||
  codepoint == 0xAAB0 ||
  (codepoint >= 0xAAB2 && codepoint <= 0xAAB4) ||
  (codepoint >= 0xAAB7 && codepoint <= 0xAAB8) ||
  (codepoint >= 0xAABE && codepoint <= 0xAABF) ||
  codepoint == 0xAAC1 ||
  (codepoint >= 0xAAEC && codepoint <= 0xAAED) ||
  codepoint == 0xABE5 ||
  codepoint == 0xABE8 ||
  codepoint == 0xABED ||
  codepoint == 0xFB1E ||
  (codepoint >= 0xFE00 && codepoint <= 0xFE0F) ||
  (codepoint >= 0xFE20 && codepoint <= 0xFE2F) ||
  (codepoint >= 0xFF9E && codepoint <= 0xFF9F) ||
  codepoint == 0x101FD ||
  codepoint == 0x102E0 ||
  (codepoint >= 0x10376 && codepoint <= 0x1037A) ||
  (codepoint >= 0x10A01 && codepoint <= 0x10A03) ||
  (codepoint >= 0x10A05 && codepoint <= 0x10A06) ||
  (codepoint >= 0x10A0C && codepoint <= 0x10A0F) ||
  (codepoint >= 0x10A38 && codepoint <= 0x10A3A) ||
  (codepoint >= 0x10AE5 && codepoint <= 0x10AE6) ||
  (codepoint >= 0x10D24 && codepoint <= 0x10D27) ||
  (codepoint >= 0x10D69 && codepoint <= 0x10D6D) ||
  (codepoint >= 0x10EAB && codepoint <= 0x10EAC) ||
  (codepoint >= 0x10EFA && codepoint <= 0x10EFF) ||
  (codepoint >= 0x10F46 && codepoint <= 0x10F50) ||
  (codepoint >= 0x10F82 && codepoint <= 0x10F85) ||
  codepoint == 0x11001 ||
  (codepoint >= 0x11038 && codepoint <= 0x11046) ||
  codepoint == 0x11070 ||
  (codepoint >= 0x11073 && codepoint <= 0x11074) ||
  (codepoint >= 0x1107F && codepoint <= 0x11081) ||
  (codepoint >= 0x110B3 && codepoint <= 0x110B6) ||
  (codepoint >= 0x110B9 && codepoint <= 0x110BA) ||
  codepoint == 0x110C2 ||
  (codepoint >= 0x11100 && codepoint <= 0x11102) ||
  (codepoint >= 0x11127 && codepoint <= 0x1112B) ||
  (codepoint >= 0x1112D && codepoint <= 0x11132)
}

///|
fn unicode_indic_conjunct_is_extend_17_0_chunk_5(codepoint : Int) -> Bool {
  codepoint == 0x11134 ||
  codepoint == 0x11173 ||
  (codepoint >= 0x11180 && codepoint <= 0x11181) ||
  (codepoint >= 0x111B6 && codepoint <= 0x111BE) ||
  codepoint == 0x111C0 ||
  (codepoint >= 0x111C9 && codepoint <= 0x111CC) ||
  codepoint == 0x111CF ||
  (codepoint >= 0x1122F && codepoint <= 0x11231) ||
  (codepoint >= 0x11234 && codepoint <= 0x11237) ||
  codepoint == 0x1123E ||
  codepoint == 0x11241 ||
  codepoint == 0x112DF ||
  (codepoint >= 0x112E3 && codepoint <= 0x112EA) ||
  (codepoint >= 0x11300 && codepoint <= 0x11301) ||
  (codepoint >= 0x1133B && codepoint <= 0x1133C) ||
  codepoint == 0x1133E ||
  codepoint == 0x11340 ||
  codepoint == 0x1134D ||
  codepoint == 0x11357 ||
  (codepoint >= 0x11366 && codepoint <= 0x1136C) ||
  (codepoint >= 0x11370 && codepoint <= 0x11374) ||
  codepoint == 0x113B8 ||
  (codepoint >= 0x113BB && codepoint <= 0x113C0) ||
  codepoint == 0x113C2 ||
  codepoint == 0x113C5 ||
  (codepoint >= 0x113C7 && codepoint <= 0x113C9) ||
  (codepoint >= 0x113CE && codepoint <= 0x113CF) ||
  codepoint == 0x113D2 ||
  (codepoint >= 0x113E1 && codepoint <= 0x113E2) ||
  (codepoint >= 0x11438 && codepoint <= 0x1143F) ||
  (codepoint >= 0x11442 && codepoint <= 0x11444) ||
  codepoint == 0x11446 ||
  codepoint == 0x1145E ||
  codepoint == 0x114B0 ||
  (codepoint >= 0x114B3 && codepoint <= 0x114B8) ||
  codepoint == 0x114BA ||
  codepoint == 0x114BD ||
  (codepoint >= 0x114BF && codepoint <= 0x114C0) ||
  (codepoint >= 0x114C2 && codepoint <= 0x114C3) ||
  codepoint == 0x115AF ||
  (codepoint >= 0x115B2 && codepoint <= 0x115B5) ||
  (codepoint >= 0x115BC && codepoint <= 0x115BD) ||
  (codepoint >= 0x115BF && codepoint <= 0x115C0) ||
  (codepoint >= 0x115DC && codepoint <= 0x115DD) ||
  (codepoint >= 0x11633 && codepoint <= 0x1163A) ||
  codepoint == 0x1163D ||
  (codepoint >= 0x1163F && codepoint <= 0x11640) ||
  codepoint == 0x116AB
}

///|
fn unicode_indic_conjunct_is_extend_17_0_chunk_6(codepoint : Int) -> Bool {
  codepoint == 0x116AD ||
  (codepoint >= 0x116B0 && codepoint <= 0x116B7) ||
  codepoint == 0x1171D ||
  codepoint == 0x1171F ||
  (codepoint >= 0x11722 && codepoint <= 0x11725) ||
  (codepoint >= 0x11727 && codepoint <= 0x1172B) ||
  (codepoint >= 0x1182F && codepoint <= 0x11837) ||
  (codepoint >= 0x11839 && codepoint <= 0x1183A) ||
  codepoint == 0x11930 ||
  (codepoint >= 0x1193B && codepoint <= 0x1193D) ||
  codepoint == 0x11943 ||
  (codepoint >= 0x119D4 && codepoint <= 0x119D7) ||
  (codepoint >= 0x119DA && codepoint <= 0x119DB) ||
  codepoint == 0x119E0 ||
  (codepoint >= 0x11A01 && codepoint <= 0x11A0A) ||
  (codepoint >= 0x11A33 && codepoint <= 0x11A38) ||
  (codepoint >= 0x11A3B && codepoint <= 0x11A3E) ||
  (codepoint >= 0x11A51 && codepoint <= 0x11A56) ||
  (codepoint >= 0x11A59 && codepoint <= 0x11A5B) ||
  (codepoint >= 0x11A8A && codepoint <= 0x11A96) ||
  codepoint == 0x11A98 ||
  codepoint == 0x11B60 ||
  (codepoint >= 0x11B62 && codepoint <= 0x11B64) ||
  codepoint == 0x11B66 ||
  (codepoint >= 0x11C30 && codepoint <= 0x11C36) ||
  (codepoint >= 0x11C38 && codepoint <= 0x11C3D) ||
  codepoint == 0x11C3F ||
  (codepoint >= 0x11C92 && codepoint <= 0x11CA7) ||
  (codepoint >= 0x11CAA && codepoint <= 0x11CB0) ||
  (codepoint >= 0x11CB2 && codepoint <= 0x11CB3) ||
  (codepoint >= 0x11CB5 && codepoint <= 0x11CB6) ||
  (codepoint >= 0x11D31 && codepoint <= 0x11D36) ||
  codepoint == 0x11D3A ||
  (codepoint >= 0x11D3C && codepoint <= 0x11D3D) ||
  (codepoint >= 0x11D3F && codepoint <= 0x11D45) ||
  codepoint == 0x11D47 ||
  (codepoint >= 0x11D90 && codepoint <= 0x11D91) ||
  codepoint == 0x11D95 ||
  codepoint == 0x11D97 ||
  (codepoint >= 0x11EF3 && codepoint <= 0x11EF4) ||
  (codepoint >= 0x11F00 && codepoint <= 0x11F01) ||
  (codepoint >= 0x11F36 && codepoint <= 0x11F3A) ||
  (codepoint >= 0x11F40 && codepoint <= 0x11F41) ||
  codepoint == 0x11F5A ||
  codepoint == 0x13440 ||
  (codepoint >= 0x13447 && codepoint <= 0x13455) ||
  (codepoint >= 0x1611E && codepoint <= 0x16129) ||
  (codepoint >= 0x1612D && codepoint <= 0x1612F)
}

///|
fn unicode_indic_conjunct_is_extend_17_0_chunk_7(codepoint : Int) -> Bool {
  (codepoint >= 0x16AF0 && codepoint <= 0x16AF4) ||
  (codepoint >= 0x16B30 && codepoint <= 0x16B36) ||
  codepoint == 0x16F4F ||
  (codepoint >= 0x16F8F && codepoint <= 0x16F92) ||
  codepoint == 0x16FE4 ||
  (codepoint >= 0x16FF0 && codepoint <= 0x16FF1) ||
  (codepoint >= 0x1BC9D && codepoint <= 0x1BC9E) ||
  (codepoint >= 0x1CF00 && codepoint <= 0x1CF2D) ||
  (codepoint >= 0x1CF30 && codepoint <= 0x1CF46) ||
  (codepoint >= 0x1D165 && codepoint <= 0x1D169) ||
  (codepoint >= 0x1D16D && codepoint <= 0x1D172) ||
  (codepoint >= 0x1D17B && codepoint <= 0x1D182) ||
  (codepoint >= 0x1D185 && codepoint <= 0x1D18B) ||
  (codepoint >= 0x1D1AA && codepoint <= 0x1D1AD) ||
  (codepoint >= 0x1D242 && codepoint <= 0x1D244) ||
  (codepoint >= 0x1DA00 && codepoint <= 0x1DA36) ||
  (codepoint >= 0x1DA3B && codepoint <= 0x1DA6C) ||
  codepoint == 0x1DA75 ||
  codepoint == 0x1DA84 ||
  (codepoint >= 0x1DA9B && codepoint <= 0x1DA9F) ||
  (codepoint >= 0x1DAA1 && codepoint <= 0x1DAAF) ||
  (codepoint >= 0x1E000 && codepoint <= 0x1E006) ||
  (codepoint >= 0x1E008 && codepoint <= 0x1E018) ||
  (codepoint >= 0x1E01B && codepoint <= 0x1E021) ||
  (codepoint >= 0x1E023 && codepoint <= 0x1E024) ||
  (codepoint >= 0x1E026 && codepoint <= 0x1E02A) ||
  codepoint == 0x1E08F ||
  (codepoint >= 0x1E130 && codepoint <= 0x1E136) ||
  codepoint == 0x1E2AE ||
  (codepoint >= 0x1E2EC && codepoint <= 0x1E2EF) ||
  (codepoint >= 0x1E4EC && codepoint <= 0x1E4EF) ||
  (codepoint >= 0x1E5EE && codepoint <= 0x1E5EF) ||
  codepoint == 0x1E6E3 ||
  codepoint == 0x1E6E6 ||
  (codepoint >= 0x1E6EE && codepoint <= 0x1E6EF) ||
  codepoint == 0x1E6F5 ||
  (codepoint >= 0x1E8D0 && codepoint <= 0x1E8D6) ||
  (codepoint >= 0x1E944 && codepoint <= 0x1E94A) ||
  (codepoint >= 0x1F3FB && codepoint <= 0x1F3FF) ||
  (codepoint >= 0xE0020 && codepoint <= 0xE007F) ||
  (codepoint >= 0xE0100 && codepoint <= 0xE01EF)
}
