# Release N Mobile Fixtures

These schema v1 configs and app-specific export maps exist only to build the
explicit Release N Android, iOS, and HarmonyOS compatibility shells. Managed
and ejected schema v2 builds must not read them. Remove this directory with the
legacy parsers and wrappers in Release N+1.
