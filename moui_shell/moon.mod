name = "wzzc-dev/moui_shell"

version = "0.1.7"

readme = "README.md"

repository = "https://github.com/wzzc-dev/MoUI"

license = "Apache-2.0"

keywords = [ "moui", "native", "shell", "embedding" ]

preferred_target = "native"

description = "MoUI native shell contract, embedders, and runners."
