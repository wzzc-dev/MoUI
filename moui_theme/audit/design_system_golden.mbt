///|
pub(all) struct DesignTokenMapping {
  preset : @common.DesignPreset
  category : DesignTokenCategory
  layer : DesignTokenLayer
  official_token : String
  official_source : String
  moui_token : String
  status : DesignTokenStatus
  note : String
} derive(Eq, Debug, ToJson)

///|
pub(all) struct DesignSourceUsage {
  preset : @common.DesignPreset
  source_label : String
  source_url : String
  source_kind : String
  source_ref : String
  token_path : String
  golden_mapping_count : Int
  source_status : DesignTokenStatus
  status : DesignTokenStatus
  note : String
} derive(Eq, Debug, ToJson)

///|
pub fn golden_mappings(
  preset : @common.DesignPreset,
) -> Array[DesignTokenMapping] {
  match preset {
    Material => material_golden_mappings()
    Carbon => carbon_golden_mappings()
    Primer => primer_golden_mappings()
    Fluent => fluent_golden_mappings()
  }
}

///|
pub fn source_usage(preset : @common.DesignPreset) -> Array[DesignSourceUsage] {
  let usages : Array[DesignSourceUsage] = []
  let mappings = golden_mappings(preset)
  for source in sources(preset) {
    let mut count = 0
    for mapping in mappings {
      if mapping.official_source == source.url {
        count = count + 1
      }
    }
    usages.push(source_usage(preset, source, count))
  }
  usages
}

///|
pub fn undeclared_golden_source_count(preset : @common.DesignPreset) -> Int {
  let mut count = 0
  let sources = sources(preset)
  for mapping in golden_mappings(preset) {
    if !source_url_declared(mapping.official_source, sources) {
      count = count + 1
    }
  }
  count
}

///|
pub fn unused_official_source_count(preset : @common.DesignPreset) -> Int {
  let mut count = 0
  for usage in source_usage(preset) {
    if usage.golden_mapping_count == 0 &&
      (
        usage.source_kind == "official-source" ||
        usage.source_kind == "official-docs"
      ) {
      count = count + 1
    }
  }
  count
}

///|
pub fn has_golden_mapping(
  preset : @common.DesignPreset,
  official_token : String,
) -> Bool {
  golden_mappings(preset).any(item => item.official_token == official_token)
}

///|
pub fn golden_mapped_count(preset : @common.DesignPreset) -> Int {
  let mut count = 0
  for item in golden_mappings(preset) {
    if item.status == DesignTokenStatus::ThemeMapped ||
      item.status == DesignTokenStatus::ComponentMapped ||
      item.status == DesignTokenStatus::OfficialComplete {
      count = count + 1
    }
  }
  count
}

///|
pub fn golden_gap_count(preset : @common.DesignPreset) -> Int {
  let mut count = 0
  for item in golden_mappings(preset) {
    if item.status == DesignTokenStatus::NotMapped ||
      item.status == DesignTokenStatus::SourceMapped ||
      item.status == DesignTokenStatus::PreviewMapped {
      count = count + 1
    }
  }
  count
}

///|
pub fn golden_mapping_for(
  preset : @common.DesignPreset,
  official_token : String,
) -> DesignTokenMapping? {
  for item in golden_mappings(preset) {
    if item.official_token == official_token {
      return Some(item)
    }
  }
  None
}

///|
fn material_golden_mappings() -> Array[DesignTokenMapping] {
  let source = "https://material-web.dev/theming/material-theming/"
  [
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Surface,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-background",
      source,
      "@common.DesignSemanticPalette.background",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI background semantic color.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Color,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-on-surface",
      source,
      "@common.DesignSemanticPalette.foreground",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI foreground semantic color.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Surface,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-surface",
      source,
      "@common.DesignSemanticPalette.surface",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI surface semantic color.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Surface,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-surface-variant",
      source,
      "@common.DesignSemanticPalette.surface_variant",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI surface-variant semantic color.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Color,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-outline",
      source,
      "@common.DesignSemanticPalette.outline",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI outline semantic color.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Color,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-on-surface-variant",
      source,
      "@common.DesignSemanticPalette.muted",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI muted/on-surface-variant semantic color.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Color,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-primary",
      source,
      "@common.DesignSemanticPalette.primary",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI primary semantic color.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Color,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-on-primary",
      source,
      "@common.DesignSemanticPalette.primary_foreground",
      DesignTokenStatus::ThemeMapped,
      "Mapped to text/icon color used on primary surfaces.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Color,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-error",
      source,
      "@common.DesignSemanticPalette.danger",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI danger semantic color; validation and feedback tokens derive from the semantic palette.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Color,
      DesignTokenLayer::SystemToken,
      "--md-sys-color-scrim",
      source,
      "@common.DesignSemanticPalette.scrim",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI scrim semantic color.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Typography,
      DesignTokenLayer::SystemToken,
      "--md-sys-typescale-body-medium-size",
      source,
      "@common.DesignTypographyTokens.body.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped into MoUI body typography slot.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Typography,
      DesignTokenLayer::SystemToken,
      "--md-sys-typescale-headline-small-size",
      source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped into MoUI title typography slot.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Typography,
      DesignTokenLayer::SystemToken,
      "--md-sys-typescale-body-small-size",
      source,
      "@common.DesignTypographyTokens.caption.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped into MoUI caption typography slot.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Typography,
      DesignTokenLayer::SystemToken,
      "--md-sys-typescale-label-large-size",
      source,
      "@common.DesignTypographyTokens.control.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped into MoUI control typography slot.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Shape,
      DesignTokenLayer::SystemToken,
      "--md-sys-shape-corner-medium",
      source,
      "@common.DesignRadiusTokens.md",
      DesignTokenStatus::ThemeMapped,
      "Mapped into MoUI medium radius slot.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Elevation,
      DesignTokenLayer::SystemToken,
      "--md-sys-elevation-level1",
      source,
      "@common.DesignElevationTokens.sm.offset.y",
      DesignTokenStatus::ThemeMapped,
      "Mapped into the neutral small elevation offset.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Elevation,
      DesignTokenLayer::SystemToken,
      "--md-sys-elevation-level3",
      source,
      "@common.DesignElevationTokens.md.offset.y",
      DesignTokenStatus::ThemeMapped,
      "Mapped into the representative neutral medium elevation offset; unrepresented Material elevation levels and tonal overlays remain adaptation gaps.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Elevation,
      DesignTokenLayer::SystemToken,
      "--md-sys-elevation-level5",
      source,
      "@common.DesignElevationTokens.lg.offset.y",
      DesignTokenStatus::ThemeMapped,
      "Mapped into the representative neutral large elevation offset; unrepresented Material elevation levels and tonal overlays remain adaptation gaps.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Motion,
      DesignTokenLayer::SystemToken,
      "--md-sys-motion-duration-short4",
      source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral fast duration; other Material duration and easing tokens remain preview-family gaps.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Motion,
      DesignTokenLayer::SystemToken,
      "--md-sys-motion-duration-medium2",
      source,
      "@common.DesignMotionTokens.normal_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral normal duration; other Material duration and easing tokens remain preview-family gaps.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Motion,
      DesignTokenLayer::SystemToken,
      "--md-sys-motion-duration-medium4",
      source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral slow duration; other Material duration and easing tokens remain preview-family gaps.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Motion,
      DesignTokenLayer::SystemToken,
      "--md-sys-motion-easing-standard",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Material cubic-bezier parity remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::Motion,
      DesignTokenLayer::SystemToken,
      "--md-sys-motion-*",
      source,
      "@common.DesignMotionTokens.normal_ms",
      DesignTokenStatus::PreviewMapped,
      "Representative Material motion duration maps to MoUI normal duration; full motion-family coverage remains preview.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--md-filled-button-container-color",
      source,
      "@common.DesignComponentTokens.button.primary.background",
      DesignTokenStatus::ComponentMapped,
      "Mapped to primary button container token.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--md-filled-button-label-text-color",
      source,
      "@common.DesignComponentTokens.button.primary.foreground",
      DesignTokenStatus::ComponentMapped,
      "Mapped to primary button label token.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::StateLayer,
      DesignTokenLayer::ComponentToken,
      "--md-filled-button-pressed-state-layer-opacity",
      source,
      "@common.DesignStateLayerTokens.pressed_alpha",
      DesignTokenStatus::ComponentMapped,
      "Mapped to shared state-layer pressed opacity.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--md-linear-progress-active-indicator-color",
      source,
      "@common.DesignComponentTokens.progress.foreground",
      DesignTokenStatus::ComponentMapped,
      "Mapped to the neutral progress active-indicator token.",
    ),
    mapping(
      @common.DesignPreset::Material,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--md-menu-container-color",
      source,
      "@common.DesignComponentTokens.picker.background",
      DesignTokenStatus::ComponentMapped,
      "Mapped to the neutral picker/date-picker container token.",
    ),
  ]
}

///|
fn carbon_golden_mappings() -> Array[DesignTokenMapping] {
  let source = "https://carbondesignsystem.com/elements/color/tokens/"
  let layout_source = "https://github.com/carbon-design-system/carbon/tree/main/packages/layout"
  let motion_source = "https://github.com/carbon-design-system/carbon/tree/main/packages/motion"
  let type_source = "https://github.com/carbon-design-system/carbon/tree/main/packages/type"
  [
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$background",
      source,
      "@common.DesignSemanticPalette.background",
      DesignTokenStatus::ThemeMapped,
      "Mapped to page background.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "$layer-01",
      source,
      "@common.DesignSemanticPalette.surface",
      DesignTokenStatus::ThemeMapped,
      "Mapped to first layer surface.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "$layer-accent-01",
      source,
      "@common.DesignSemanticPalette.surface_variant",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the alternate surface semantic color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$text-primary",
      source,
      "@common.DesignSemanticPalette.foreground",
      DesignTokenStatus::ThemeMapped,
      "Mapped to primary text color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$text-secondary",
      source,
      "@common.DesignSemanticPalette.muted",
      DesignTokenStatus::ThemeMapped,
      "Mapped to muted secondary text color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$text-on-color",
      source,
      "@common.DesignSemanticPalette.primary_foreground",
      DesignTokenStatus::ThemeMapped,
      "Mapped to foreground color used on primary/color surfaces.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$border-strong-01",
      source,
      "@common.DesignSemanticPalette.outline",
      DesignTokenStatus::ThemeMapped,
      "Mapped to outline semantic color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$background-brand",
      source,
      "@common.DesignSemanticPalette.primary",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI primary semantic color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$support-error",
      source,
      "@common.DesignSemanticPalette.danger",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI danger semantic color; validation and feedback tokens derive from the semantic palette.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$support-success",
      source,
      "@common.DesignSemanticPalette.success",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI success semantic color; feedback tokens derive from the semantic palette.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$support-warning",
      source,
      "@common.DesignSemanticPalette.warning",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI warning semantic color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$support-info",
      source,
      "@common.DesignSemanticPalette.info",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI info semantic color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$focus",
      source,
      "@common.DesignSemanticPalette.focus",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI focus semantic color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "$overlay",
      source,
      "@common.DesignSemanticPalette.scrim",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI scrim/overlay semantic color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "$body-02",
      type_source,
      "@common.DesignTypographyTokens.body.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to neutral body typography size.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "$productive-heading-03",
      type_source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to neutral title typography size.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "$caption-01",
      type_source,
      "@common.DesignTypographyTokens.caption.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to neutral caption typography size.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "$label-02",
      type_source,
      "@common.DesignTypographyTokens.control.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to neutral control typography size.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "$button-primary",
      source,
      "@common.DesignComponentTokens.button.primary.background",
      DesignTokenStatus::ComponentMapped,
      "Mapped to primary button container token.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::StateLayer,
      DesignTokenLayer::AliasToken,
      "$background-hover",
      source,
      "@common.DesignStateLayerTokens.hover_alpha",
      DesignTokenStatus::ComponentMapped,
      "Mapped as shared hover-state opacity.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "$border-interactive",
      source,
      "@common.DesignTextFieldTokens.focused.border",
      DesignTokenStatus::ComponentMapped,
      "Mapped to focused input border/focus color.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "$slider-filled-track",
      source,
      "@common.DesignComponentTokens.slider.foreground",
      DesignTokenStatus::ComponentMapped,
      "Mapped to the neutral slider filled-track token.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-01",
      layout_source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-small spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-02",
      layout_source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the nearest neutral extra-small spacing slot; Carbon spacing-02 is 4px while the Carbon adapter's current xs slot is 2px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-03",
      layout_source,
      "@common.DesignSpacingTokens.sm",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral small spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-04",
      layout_source,
      "@common.DesignSpacingTokens.md",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral medium spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-05",
      layout_source,
      "@common.DesignSpacingTokens.lg",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-06",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-07",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon spacing-07 is 32px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-08",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon spacing-08 is 40px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-09",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon spacing-09 is 48px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-10",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon spacing-10 is 64px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-11",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon spacing-11 is 80px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-12",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon spacing-12 is 96px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$spacing-13",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon spacing-13 is 160px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$layout-01",
      layout_source,
      "@common.DesignSpacingTokens.lg",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$layout-02",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$layout-03",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon layout-03 is 32px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$layout-04",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon layout-04 is 48px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$layout-05",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon layout-05 is 64px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$layout-06",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon layout-06 is 96px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$layout-07",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon layout-07 is 160px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$container-01",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$container-02",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon container-02 is 32px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$container-03",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon container-03 is 40px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$container-04",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon container-04 is 48px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "$container-05",
      layout_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into the neutral extra-large spacing slot; Carbon container-05 is 64px while the current xl slot is 24px.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "$duration-fast-01",
      motion_source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral fast duration; Carbon fast-01 remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "$duration-fast-02",
      motion_source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral fast duration; Carbon fast-02 is pinned while MoUI's current fast slot remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "$duration-moderate-01",
      motion_source,
      "@common.DesignMotionTokens.normal_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral normal duration.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "$duration-moderate-02",
      motion_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral slow duration used by the Carbon preview adapter.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "$duration-slow-01",
      motion_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Carbon slow-01 remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "$duration-slow-02",
      motion_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Carbon slow-02 remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "easings.standard.productive",
      motion_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Carbon cubic-bezier parity remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "easings.standard.expressive",
      motion_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Carbon expressive standard curve remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "easings.entrance.productive",
      motion_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Carbon productive entrance curve remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "easings.entrance.expressive",
      motion_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Carbon expressive entrance curve remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "easings.exit.productive",
      motion_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Carbon productive exit curve remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "easings.exit.expressive",
      motion_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Carbon expressive exit curve remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "Carbon motion token family",
      motion_source,
      "@common.DesignMotionTokens",
      DesignTokenStatus::PreviewMapped,
      "Representative duration and easing tokens are imported; full Carbon motion token import is pending.",
    ),
    mapping(
      @common.DesignPreset::Carbon,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::AliasToken,
      "$field",
      source,
      "@common.DesignComponentTokens.picker.background",
      DesignTokenStatus::ComponentMapped,
      "Mapped to the neutral picker/date-picker field background token.",
    ),
  ]
}

///|
fn primer_golden_mappings() -> Array[DesignTokenMapping] {
  let source = "https://primer.style/product/primitives/color/"
  let primitives_source = "https://primer.style/primitives/"
  [
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--fgColor-default",
      source,
      "@common.DesignSemanticPalette.foreground",
      DesignTokenStatus::ThemeMapped,
      "Mapped to foreground semantic color.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--fgColor-muted",
      source,
      "@common.DesignSemanticPalette.muted",
      DesignTokenStatus::ThemeMapped,
      "Mapped to muted semantic text color.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--fgColor-accent",
      source,
      "@common.DesignSemanticPalette.info",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI info semantic color; Primer documents accent text for informational usage.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "--bgColor-accent-emphasis",
      source,
      "@common.DesignSemanticPalette.primary",
      DesignTokenStatus::ThemeMapped,
      "Mapped to primary semantic color using Primer's emphasized accent background.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--fgColor-onEmphasis",
      source,
      "@common.DesignSemanticPalette.primary_foreground",
      DesignTokenStatus::ThemeMapped,
      "Mapped to foreground color used on emphasis/primary fills.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--fgColor-danger",
      source,
      "@common.DesignSemanticPalette.danger",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI danger semantic color; validation and feedback tokens derive from the semantic palette.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--fgColor-success",
      source,
      "@common.DesignSemanticPalette.success",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI success semantic color; feedback tokens derive from the semantic palette.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--fgColor-attention",
      source,
      "@common.DesignSemanticPalette.warning",
      DesignTokenStatus::ThemeMapped,
      "Mapped to MoUI warning semantic color.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "--bgColor-default",
      source,
      "@common.DesignSemanticPalette.background",
      DesignTokenStatus::ThemeMapped,
      "Mapped to app background.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "--bgColor-muted",
      source,
      "@common.DesignSemanticPalette.surface",
      DesignTokenStatus::ThemeMapped,
      "Mapped to neutral surface color.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "--bgColor-inset",
      source,
      "@common.DesignSemanticPalette.surface_variant",
      DesignTokenStatus::ThemeMapped,
      "Mapped to neutral surface-variant color.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--borderColor-default",
      source,
      "@common.DesignSemanticPalette.outline",
      DesignTokenStatus::ThemeMapped,
      "Mapped to outline semantic color.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "--borderColor-accent-emphasis",
      source,
      "@common.DesignSemanticPalette.focus",
      DesignTokenStatus::ThemeMapped,
      "Mapped to focus semantic color using Primer's emphasized accent border.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--overlay-backdrop-bgColor",
      source,
      "@common.DesignSemanticPalette.scrim",
      DesignTokenStatus::ThemeMapped,
      "Mapped to scrim semantic color using Primer's overlay backdrop token.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--button-primary-bgColor-rest",
      source,
      "@common.DesignComponentTokens.button.primary.background",
      DesignTokenStatus::ComponentMapped,
      "Mapped to primary button container token.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::StateLayer,
      DesignTokenLayer::ComponentToken,
      "--control-transparent-bgColor-hover",
      source,
      "@common.DesignStateLayerTokens.hover_alpha",
      DesignTokenStatus::ComponentMapped,
      "Mapped as shared hover-state opacity.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--control-borderColor-emphasis",
      source,
      "@common.DesignTextFieldTokens.focused.border",
      DesignTokenStatus::ComponentMapped,
      "Mapped to focused control border.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--progressBar-fgColor-accent",
      source,
      "@common.DesignComponentTokens.progress.foreground",
      DesignTokenStatus::ComponentMapped,
      "Mapped to the neutral progress foreground token.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::ReferenceToken,
      "--base-size-4",
      primitives_source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-small spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::ReferenceToken,
      "--base-size-8",
      primitives_source,
      "@common.DesignSpacingTokens.sm",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral small spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::ReferenceToken,
      "--base-size-12",
      primitives_source,
      "@common.DesignSpacingTokens.md",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral medium spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::ReferenceToken,
      "--base-size-16",
      primitives_source,
      "@common.DesignSpacingTokens.lg",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::ReferenceToken,
      "--base-size-24",
      primitives_source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Typography,
      DesignTokenLayer::ReferenceToken,
      "--base-text-size-xs",
      primitives_source,
      "@common.DesignTypographyTokens.caption.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral caption text size slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Typography,
      DesignTokenLayer::ReferenceToken,
      "--base-text-size-sm",
      primitives_source,
      "@common.DesignTypographyTokens.body.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral body text size slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Typography,
      DesignTokenLayer::ReferenceToken,
      "--base-text-size-lg",
      primitives_source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral title text size slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Typography,
      DesignTokenLayer::ReferenceToken,
      "--base-text-weight-normal",
      primitives_source,
      "@common.DesignTypographyTokens.body.weight",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral body text weight slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Typography,
      DesignTokenLayer::ReferenceToken,
      "--base-text-weight-semibold",
      primitives_source,
      "@common.DesignTypographyTokens.control.weight",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral control text weight slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "--borderRadius-small",
      primitives_source,
      "@common.DesignRadiusTokens.sm",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral small radius slot; Primer small is 3px while MoUI currently uses 4px.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "--borderRadius-medium",
      primitives_source,
      "@common.DesignRadiusTokens.md",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral medium radius slot.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "--borderRadius-large",
      primitives_source,
      "@common.DesignRadiusTokens.lg",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral large radius slot; Primer large is 12px while MoUI currently uses 8px.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "--borderRadius-full",
      primitives_source,
      "@common.DesignRadiusTokens.pill",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral pill radius slot; Primer full is 9999px while MoUI currently uses 999px.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "--borderRadius-default",
      primitives_source,
      "@common.DesignRadiusTokens.md",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral medium radius slot through Primer's default alias.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Elevation,
      DesignTokenLayer::AliasToken,
      "--shadow-resting-small",
      primitives_source,
      "@common.DesignElevationTokens.sm.offset.y",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral small elevation offset; Primer's full multi-layer shadow remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Elevation,
      DesignTokenLayer::AliasToken,
      "--shadow-resting-medium",
      primitives_source,
      "@common.DesignElevationTokens.md.offset.y",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the representative neutral medium elevation offset; Primer's two-layer shadow is not represented exactly.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Elevation,
      DesignTokenLayer::AliasToken,
      "--shadow-floating-large",
      primitives_source,
      "@common.DesignElevationTokens.lg.offset.y",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the representative neutral large elevation offset; Primer floating shadows and overlays remain adaptation gaps.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-0",
      primitives_source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral fast duration; Primer 0ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-50",
      primitives_source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral fast duration; Primer 50ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-100",
      primitives_source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral fast duration; broader Primer animation primitives remain preview.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-200",
      primitives_source,
      "@common.DesignMotionTokens.normal_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral normal duration; MoUI currently keeps a shorter default.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-300",
      primitives_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; MoUI currently keeps a shorter default.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-400",
      primitives_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Primer 400ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-500",
      primitives_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Primer 500ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-600",
      primitives_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Primer 600ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-700",
      primitives_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Primer 700ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-800",
      primitives_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Primer 800ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-900",
      primitives_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Primer 900ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-duration-1000",
      primitives_source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Primer 1000ms remains an adaptation difference.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-easing-linear",
      primitives_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Primer linear curve parity remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-easing-ease",
      primitives_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Primer ease curve parity remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-easing-easeIn",
      primitives_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Primer easeIn curve parity remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-easing-easeOut",
      primitives_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Primer cubic-bezier parity remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "--base-easing-easeInOut",
      primitives_source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; exact Primer easeInOut curve parity remains an adaptation gap.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "Primer animation token family",
      source,
      "@common.DesignMotionTokens",
      DesignTokenStatus::PreviewMapped,
      "Duration/easing slots exist; full Primer animation token import is pending.",
    ),
    mapping(
      @common.DesignPreset::Primer,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "--control-bgColor-rest",
      source,
      "@common.DesignComponentTokens.picker.background",
      DesignTokenStatus::ComponentMapped,
      "Mapped to the neutral picker/date-picker background token.",
    ),
  ]
}

///|
fn fluent_golden_mappings() -> Array[DesignTokenMapping] {
  let source = "https://fluent2.microsoft.design/design-tokens"
  let component_source = "https://github.com/microsoft/fluentui/tree/master/packages/web-components"
  [
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "colorNeutralForeground1",
      source,
      "@common.DesignSemanticPalette.foreground",
      DesignTokenStatus::ThemeMapped,
      "Mapped to foreground semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "colorNeutralForeground3",
      source,
      "@common.DesignSemanticPalette.muted",
      DesignTokenStatus::ThemeMapped,
      "Mapped to muted secondary foreground semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "colorNeutralBackground1",
      source,
      "@common.DesignSemanticPalette.background",
      DesignTokenStatus::ThemeMapped,
      "Mapped to app background.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "colorNeutralBackground2",
      source,
      "@common.DesignSemanticPalette.surface",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral surface semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Surface,
      DesignTokenLayer::AliasToken,
      "colorNeutralBackground3",
      source,
      "@common.DesignSemanticPalette.surface_variant",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral surface-variant semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "colorNeutralStroke1",
      source,
      "@common.DesignSemanticPalette.outline",
      DesignTokenStatus::ThemeMapped,
      "Mapped to outline semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "colorBrandBackground",
      source,
      "@common.DesignSemanticPalette.primary",
      DesignTokenStatus::ThemeMapped,
      "Mapped to primary semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "colorNeutralForegroundOnBrand",
      source,
      "@common.DesignSemanticPalette.primary_foreground",
      DesignTokenStatus::ThemeMapped,
      "Mapped to foreground color used on brand/primary backgrounds.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "colorBackgroundOverlay",
      source,
      "@common.DesignSemanticPalette.scrim",
      DesignTokenStatus::ThemeMapped,
      "Mapped to scrim/overlay semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::AliasToken,
      "colorStrokeFocus2",
      source,
      "@common.DesignSemanticPalette.focus",
      DesignTokenStatus::ThemeMapped,
      "Mapped to focus semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "red.primary",
      source,
      "@common.DesignSemanticPalette.danger",
      DesignTokenStatus::ThemeMapped,
      "Mapped to danger semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "green.primary",
      source,
      "@common.DesignSemanticPalette.success",
      DesignTokenStatus::ThemeMapped,
      "Mapped to success semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "lightGreen.primary",
      source,
      "@common.DesignSemanticPalette.success",
      DesignTokenStatus::PreviewMapped,
      "Mapped to success semantic color; Fluent light-green primary differs from MoUI's current neutral success color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "cranberry.primary",
      source,
      "@common.DesignSemanticPalette.danger",
      DesignTokenStatus::PreviewMapped,
      "Mapped to danger semantic color; Fluent cranberry primary differs from MoUI's current neutral danger color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "darkOrange.primary",
      source,
      "@common.DesignSemanticPalette.warning",
      DesignTokenStatus::ThemeMapped,
      "Mapped to warning semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "orange.primary",
      source,
      "@common.DesignSemanticPalette.warning",
      DesignTokenStatus::PreviewMapped,
      "Mapped to warning semantic color; Fluent orange primary differs from MoUI's current neutral warning color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "yellow.primary",
      source,
      "@common.DesignSemanticPalette.warning",
      DesignTokenStatus::PreviewMapped,
      "Mapped to warning semantic color; Fluent yellow primary differs from MoUI's current neutral warning color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "marigold.primary",
      source,
      "@common.DesignSemanticPalette.warning",
      DesignTokenStatus::PreviewMapped,
      "Mapped to warning semantic color; Fluent marigold primary differs from MoUI's current neutral warning color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Color,
      DesignTokenLayer::ReferenceToken,
      "blue.primary",
      source,
      "@common.DesignSemanticPalette.info",
      DesignTokenStatus::ThemeMapped,
      "Mapped to info semantic color.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeBase100",
      source,
      "@common.DesignTypographyTokens.caption.size",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into caption typography slot; Fluent 10px differs from MoUI's current 12px caption size.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeBase200",
      source,
      "@common.DesignTypographyTokens.caption.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to caption typography slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeBase300",
      source,
      "@common.DesignTypographyTokens.body.size",
      DesignTokenStatus::ThemeMapped,
      "Mapped to body typography slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeBase400",
      source,
      "@common.DesignTypographyTokens.control.size",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into control typography slot; Fluent 16px differs from MoUI's current 14px control size.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeBase500",
      source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::PreviewMapped,
      "Mapped to title typography slot; Fluent 20px differs from MoUI's current 22px title size.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeBase600",
      source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into title typography slot; Fluent 24px differs from MoUI's current 22px title size.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeHero700",
      source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into title typography slot; Fluent hero sizes need a richer neutral type ramp.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeHero800",
      source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into title typography slot; Fluent hero sizes need a richer neutral type ramp.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeHero900",
      source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into title typography slot; Fluent hero sizes need a richer neutral type ramp.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontSizeHero1000",
      source,
      "@common.DesignTypographyTokens.title.size",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into title typography slot; Fluent hero sizes need a richer neutral type ramp.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightBase100",
      source,
      "@common.DesignTypographyTokens.caption.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightBase200",
      source,
      "@common.DesignTypographyTokens.caption.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightBase300",
      source,
      "@common.DesignTypographyTokens.body.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightBase400",
      source,
      "@common.DesignTypographyTokens.control.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightBase500",
      source,
      "@common.DesignTypographyTokens.title.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightBase600",
      source,
      "@common.DesignTypographyTokens.title.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightHero700",
      source,
      "@common.DesignTypographyTokens.title.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightHero800",
      source,
      "@common.DesignTypographyTokens.title.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightHero900",
      source,
      "@common.DesignTypographyTokens.title.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "lineHeightHero1000",
      source,
      "@common.DesignTypographyTokens.title.line_height",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview line-height destination; neutral typography does not expose line-height yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontWeightRegular",
      source,
      "@common.DesignTypographyTokens.body.weight",
      DesignTokenStatus::ThemeMapped,
      "Mapped to body typography weight.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontWeightMedium",
      source,
      "@common.DesignTypographyTokens.control.weight",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into control typography weight; Fluent 500 differs from MoUI's current 600 control weight.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontWeightSemibold",
      source,
      "@common.DesignTypographyTokens.control.weight",
      DesignTokenStatus::ThemeMapped,
      "Mapped to control typography weight.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontWeightBold",
      source,
      "@common.DesignTypographyTokens.title.weight",
      DesignTokenStatus::ThemeMapped,
      "Mapped to title typography weight.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontFamilyBase",
      source,
      "@common.DesignTypographyTokens.body.families",
      DesignTokenStatus::PreviewMapped,
      "Recorded as a preview font-family destination; neutral Fluent tokens still default to system-ui.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontFamilyMonospace",
      source,
      "@common.DesignTypographyTokens.control.families",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into control font families; MoUI has no dedicated monospace typography slot yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Typography,
      DesignTokenLayer::AliasToken,
      "fontFamilyNumeric",
      source,
      "@common.DesignTypographyTokens.body.families",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into body font families; MoUI has no dedicated numeric typography slot yet.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::AliasToken,
      "strokeWidthThin",
      source,
      "@common.DesignComponentTokens.text_field.normal.border_width",
      DesignTokenStatus::ComponentMapped,
      "Mapped to neutral one-pixel text-field border width.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::AliasToken,
      "strokeWidthThick",
      source,
      "@common.DesignComponentTokens.text_field.focused.border_width",
      DesignTokenStatus::ComponentMapped,
      "Mapped to neutral two-pixel focused text-field border width.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::AliasToken,
      "strokeWidthThicker",
      source,
      "@common.DesignComponentTokens.text_field.focused.border_width",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into focused text-field border width; Fluent 3px differs from MoUI's current 2px focused border.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::AliasToken,
      "strokeWidthThickest",
      source,
      "@common.DesignComponentTokens.text_field.focused.border_width",
      DesignTokenStatus::PreviewMapped,
      "Collapsed into focused text-field border width; Fluent 4px differs from MoUI's current 2px focused border.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadiusNone",
      source,
      "@common.DesignRadiusTokens.sm",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the smallest neutral radius slot; Fluent none is 0px while MoUI currently uses 2px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadiusMedium",
      source,
      "@common.DesignRadiusTokens.md",
      DesignTokenStatus::ThemeMapped,
      "Mapped to medium radius slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadiusSmall",
      source,
      "@common.DesignRadiusTokens.sm",
      DesignTokenStatus::ThemeMapped,
      "Mapped to small radius slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadiusLarge",
      source,
      "@common.DesignRadiusTokens.lg",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral large radius slot; Fluent large is 6px while MoUI currently uses 8px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadiusXLarge",
      source,
      "@common.DesignRadiusTokens.lg",
      DesignTokenStatus::ThemeMapped,
      "Mapped to large radius slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadius2XLarge",
      source,
      "@common.DesignRadiusTokens.lg",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral large radius slot; Fluent 2XL is 12px while MoUI currently uses 8px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadius3XLarge",
      source,
      "@common.DesignRadiusTokens.lg",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral large radius slot; Fluent 3XL is 16px while MoUI currently uses 8px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadius4XLarge",
      source,
      "@common.DesignRadiusTokens.pill",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral pill radius slot; Fluent 4XL is 24px while MoUI currently uses 999px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadius5XLarge",
      source,
      "@common.DesignRadiusTokens.pill",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral pill radius slot; Fluent 5XL is 32px while MoUI currently uses 999px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadius6XLarge",
      source,
      "@common.DesignRadiusTokens.pill",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral pill radius slot; Fluent 6XL is 40px while MoUI currently uses 999px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Shape,
      DesignTokenLayer::AliasToken,
      "borderRadiusCircular",
      source,
      "@common.DesignRadiusTokens.pill",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral pill sentinel; Fluent circular radius is 10000px while MoUI currently uses 999px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "colorCompoundBrandBackground",
      source,
      "@common.DesignComponentTokens.button.primary.background",
      DesignTokenStatus::ComponentMapped,
      "Mapped to primary action background.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::StateLayer,
      DesignTokenLayer::AliasToken,
      "colorNeutralBackground1Hover",
      source,
      "@common.DesignStateLayerTokens.hover_alpha",
      DesignTokenStatus::ComponentMapped,
      "Mapped as shared hover-state opacity.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::ComponentToken,
      "ProgressBar.indicator.backgroundColor",
      component_source,
      "@common.DesignComponentTokens.progress.foreground",
      DesignTokenStatus::ComponentMapped,
      "Mapped from the Fluent progress-bar indicator background rule.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "durationUltraFast",
      source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral fast duration; Fluent ultra-fast is 50ms while MoUI currently uses 100ms.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "durationFaster",
      source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral fast duration.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "durationFast",
      source,
      "@common.DesignMotionTokens.fast_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral fast duration; Fluent fast is 150ms while MoUI currently uses 100ms.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "durationNormal",
      source,
      "@common.DesignMotionTokens.normal_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral normal duration; full Fluent duration and curve coverage remains a preview gap.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "durationGentle",
      source,
      "@common.DesignMotionTokens.normal_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral normal duration; Fluent gentle is 250ms while MoUI currently uses 200ms.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "durationSlow",
      source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral slow duration.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "durationSlower",
      source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Fluent slower is 400ms while MoUI currently uses 300ms.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "durationUltraSlow",
      source,
      "@common.DesignMotionTokens.slow_ms",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral slow duration; Fluent ultra-slow is 500ms while MoUI currently uses 300ms.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalNone",
      source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the smallest neutral spacing slot; Fluent none is 0px while MoUI currently uses 4px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalXXS",
      source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the nearest neutral extra-small spacing slot; Fluent XXS is 2px while MoUI currently uses 4px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalM",
      source,
      "@common.DesignSpacingTokens.md",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral medium spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalXS",
      source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-small spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalSNudge",
      source,
      "@common.DesignSpacingTokens.sm",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the nearest neutral small spacing slot; Fluent small nudge is 6px while MoUI currently uses 8px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalS",
      source,
      "@common.DesignSpacingTokens.sm",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral small spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalMNudge",
      source,
      "@common.DesignSpacingTokens.md",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the nearest neutral medium spacing slot; Fluent medium nudge is 10px while MoUI currently uses 12px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalL",
      source,
      "@common.DesignSpacingTokens.lg",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral large spacing slot; Fluent large is 16px while MoUI currently uses 20px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalXL",
      source,
      "@common.DesignSpacingTokens.lg",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalXXL",
      source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral extra-large spacing slot; Fluent XXL is 24px while MoUI currently uses 32px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingHorizontalXXXL",
      source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalNone",
      source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the smallest neutral spacing slot; Fluent vertical none is 0px while MoUI currently uses 4px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalXXS",
      source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the nearest neutral extra-small spacing slot; Fluent vertical XXS is 2px while MoUI currently uses 4px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalXS",
      source,
      "@common.DesignSpacingTokens.xs",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-small spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalSNudge",
      source,
      "@common.DesignSpacingTokens.sm",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the nearest neutral small spacing slot; Fluent vertical small nudge is 6px while MoUI currently uses 8px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalS",
      source,
      "@common.DesignSpacingTokens.sm",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral small spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalMNudge",
      source,
      "@common.DesignSpacingTokens.md",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the nearest neutral medium spacing slot; Fluent vertical medium nudge is 10px while MoUI currently uses 12px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalM",
      source,
      "@common.DesignSpacingTokens.md",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral medium spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalL",
      source,
      "@common.DesignSpacingTokens.lg",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral large spacing slot; Fluent vertical large is 16px while MoUI currently uses 20px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalXL",
      source,
      "@common.DesignSpacingTokens.lg",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalXXL",
      source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral extra-large spacing slot; Fluent vertical XXL is 24px while MoUI currently uses 32px.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Spacing,
      DesignTokenLayer::AliasToken,
      "spacingVerticalXXXL",
      source,
      "@common.DesignSpacingTokens.xl",
      DesignTokenStatus::ThemeMapped,
      "Mapped to the neutral extra-large spacing slot.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveAccelerateMax",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent maximum acceleration curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveAccelerateMid",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent medium acceleration curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveAccelerateMin",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent minimum acceleration curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveDecelerateMax",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent maximum deceleration curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveDecelerateMid",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent medium deceleration curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveDecelerateMin",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent minimum deceleration curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveEasyEaseMax",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent maximum easy-ease curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveEasyEase",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent cubic-bezier curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::Motion,
      DesignTokenLayer::AliasToken,
      "curveLinear",
      source,
      "@common.DesignMotionTokens.easing",
      DesignTokenStatus::PreviewMapped,
      "Mapped to the neutral easing slot; the exact Fluent linear curve remains a preview adaptation.",
    ),
    mapping(
      @common.DesignPreset::Fluent,
      DesignTokenCategory::ComponentStyle,
      DesignTokenLayer::AliasToken,
      "colorNeutralBackground1",
      source,
      "@common.DesignComponentTokens.picker.background",
      DesignTokenStatus::ComponentMapped,
      "Mapped to the neutral picker/date-picker background token.",
    ),
  ]
}

///|
fn mapping(
  preset : @common.DesignPreset,
  category : DesignTokenCategory,
  layer : DesignTokenLayer,
  official_token : String,
  official_source : String,
  moui_token : String,
  status : DesignTokenStatus,
  note : String,
) -> DesignTokenMapping {
  {
    preset,
    category,
    layer,
    official_token,
    official_source,
    moui_token,
    status,
    note,
  }
}

///|
fn source_usage(
  preset : @common.DesignPreset,
  source : DesignSystemSource,
  count : Int,
) -> DesignSourceUsage {
  let status = if count > 0 {
    if source.source_kind == "internal-token-contract" ||
      source.source_kind == "internal-component-adapter" {
      DesignTokenStatus::ThemeMapped
    } else {
      DesignTokenStatus::SourceMapped
    }
  } else {
    DesignTokenStatus::NotMapped
  }
  {
    preset,
    source_label: source.label,
    source_url: source.url,
    source_kind: source.source_kind,
    source_ref: source.source_ref,
    token_path: source.token_path,
    golden_mapping_count: count,
    source_status: source.status,
    status,
    note: if count > 0 {
      count.to_string() + " golden mappings cite this source."
    } else {
      "No golden mappings cite this source yet."
    },
  }
}

///|
fn source_url_declared(
  url : String,
  sources : Array[DesignSystemSource],
) -> Bool {
  sources.any(source => source.url == url)
}
